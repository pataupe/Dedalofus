# CLAUDE.md — Dédale-Book

Fichier de contexte projet pour Claude Code. À placer à la racine du repo (`Dedalofus/`).

## Profil du développeur

- Solo dev, ~2 ans d'expérience en Git, React, terminal, JavaScript, HTML, CSS, Node.js, Express, SQL.
- Pas de sur-explication du jargon de base nécessaire. Reste concret et direct.
- MySQL : niveau à confirmer/vérifier en début de session si besoin, pas encore pratiqué en profondeur sur ce projet.

## Le projet en une phrase

Un simulateur en ligne permettant aux joueurs de Dofus Touch de choisir un équipement complet (Dédale) pour leur personnage et de calculer les dégâts obtenus, en prévision d'un run du donjon "Dédale". Aucun outil équivalent n'existe actuellement (DofusBook ne couvre pas le Dédale) — le porteur de projet est seul sur ce créneau, d'où une priorité forte sur la rapidité de sortie du MVP.

## Stack technique

- **Frontend** : React (Vite, JavaScript, ESLint)
- **Backend** : Node.js + Express
- **Base de données** : MySQL — pas d'ORM, requêtes SQL écrites à la main
- **Pas de Next.js** : aucun bénéfice décisif, coût d'apprentissage non justifié
- Express + React n'imposent pas MongoDB ("MERN" est une convention de tutoriel, pas une contrainte) : MySQL est choisi car le modèle de données est fortement relationnel (utilisateur → personnages → stuffs → emplacements référençant cubes/breloques/sorts).

## Structure du repo (mono-repo)

```
Dedalofus/
  client/     → React (Vite)
  server/     → Express
```

- `server/index.js` : point d'entrée Express (CORS + `express.json()`), monte tous les routers + middleware d'erreur global
- `server/.env` : `PORT=3001` + `DB_HOST`, `DB_USER`, `DB_PASSWORD`, `DB_NAME=dedalofus` (connexion MySQL locale) + `JWT_SECRET`
- `server/scripts/` : scripts d'import Node (`import-cubes.js`, `import-breloques.js`, `import-sorts.js`), dépendent de `mysql2` et `csv-parse`
- `schema.sql` (racine du repo) : script de création de la base `dedalofus` et de ses tables (Utilisateur avec `pseudo` unique inclus), à exécuter une seule fois
- `server/config/db.js` : pool de connexions MySQL (`mysql2/promise`)
- `server/logic/calcul.js` (+ `calcul.test.js`) : module de calcul de dégâts, 2 fonctions pures (Tâche 3)
- `server/controllers/` + `server/routes/` : `cubesController`/`breloquesController`/`sortsController` (API lecture seule, Tâche 4) + `authController`/routes `auth` (inscription/connexion, Tâche 6) + `personnagesController`/routes `personnages` (création/liste de personnages, Tâche 7 bout 1)
- `server/middleware/verifierToken.js` : middleware JWT, pose `req.utilisateur` (`{ id, email, pseudo }`) — branché sur les routes `personnages` depuis la Tâche 7 (premier usage)
- `client/src/pages/` : `HomePage`, `CubeListPage`/`CubeDetailPage`, `BreloqueListPage`, `SortListPage`, `ConnexionPage`, `InscriptionPage`, `PersonnagePage` (liste/création de personnages, nom seul — Tâche 7 bout 1, pas encore d'emplacements d'équipement)
- `client/src/components/` : `Header` (nav + état connexion, lien "Personnage" affiché si connecté), `CubeCard`, `BreloqueCard`, `SortCard`
- `client/src/api/` : wrappers fetch (`cubes.js`, `breloques.js`, `sorts.js`, `auth.js`, `personnages.js`)
- `client/src/context/AuthContext.jsx` : session (token + utilisateur) persistée dans `localStorage`
- `client/src/constants/` : `elements.js`, `rangs.js` (cubes), `elementsSorts.js`, `rangsMaitrise.js` (Novice/Expert/Maître α/Maître ẞ, partagé breloques+sorts), `statsCubes.js` (35 stats vérifiées en base)
- `client/src/assets/logo.webp` : logo du site
- Thème sombre : fond `#4A433B`, variables couleur par élément dans `client/src/index.css`
- `.claude/launch.json` : config pour prévisualiser le client (`npm run dev`, port 5173) via l'outil de preview

## Périmètre du MVP

Trois blocs indispensables :

1. **Liste des équipements du Dédale** — consultable sans compte, recherche par nom, filtre par type (pas de filtre par niveau, ça n'existe pas en Dédale)
2. **Comptes utilisateurs** — création de personnage (pas de classe, pas de niveau, juste un stuff équipé) et sauvegarde de stuffs
3. **Calculateur de dégâts** appliqué aux stuffs

**Explicitement repoussé après le MVP** : liste des mobs, articles tuto, comparateur d'items avancé, système de communauté, images réelles des équipements (visuel générique par type/élément en V1).

**Authentification** : site bénévole, pas de données bancaires. Un JWT simple + bcrypt suffit, pas de niveau de sécurité bancaire nécessaire.

## Les 4 types d'équipements

### Cubes
- Donnent des stats (ex: Intelligence, Agilité, Force, Chance, Vitalité, Puissance...)
- 6 éléments possibles, 5 rangs d'évolution : Commun, Rare, Épique, Mythique, Éxalté
- Pour une combinaison élément + rang donnée, les stats sont **fixes** (pas de fourchette aléatoire)
- 420 cubes au total, données déjà disponibles dans `cubes_v2.json`
- **9 cubes équipés** par personnage

### Breloques
- **7 emplacements** par personnage
- ⚠️ **Règle historique abandonnée** : ce document disait autrefois "effet purement informatif, sauf une seule exception calculée" (Tâche 1). Périmé depuis l'ajout de l'onglet "Boosts breloques" (voir "État d'avancement") — désormais potentiellement **une trentaine de breloques** ont un bonus conditionnel réellement paramétrable par le joueur (toggle/range/accumulateur), stocké et borné côté serveur. Rien à voir avec un cas particulier isolé.
- Données de référence dans `DEDALE BRELOQUES.csv` (`Nom`/`Rang`/`Effet`, historique) puis `DEDALE - BRELOQUES_NEW.csv` (ajoute `Tag`/`Ensemble lié`/les 4 colonnes de bonus conditionnel — voir "Onglet Boosts breloques" ci-dessous pour le détail des colonnes `Breloque` ajoutées).

### Breuvages
- **Nouveau 4e type d'équipement**, décidé en Tâche 7 (n'existait pas dans les blocs MVP d'origine)
- **3 emplacements** par personnage (colonne de droite sur la maquette `maquetteEquipementPerso.png`)
- **10 familles, 3 rangs chacune (Petit/Normal/Grand)** — confirmé, pas de 4e rang comme les cubes/breloques/sorts. Chaque breuvage ne boost qu'**une seule stat fixe** (ex: Breuvage Stimulant → Intelligence, valeurs 50/100/150 Petit/Normal/Grand) — voir la table complète dans "Breuvages — table de données" plus bas. Rang affiché avec des étoiles sur les icônes (1/2/3 étoiles Petit/Normal/Grand), confirmant l'hypothèse de rang à étoiles notée initialement ici.
- **Table `Breuvage`** (référentiel, 30 lignes) au lieu d'emplacements vides fictifs — voir "Onglet Breuvages" dans "État d'avancement" pour le détail de l'implémentation complète (base, API, page liste, branchement fiche perso).

### Sorts
- **9 sorts équipés** par personnage
- Liste fermée indépendante de toute classe (pas de notion Iop/Cra/etc.)
- Chaque sort a un élément et des dégâts de base fixes (ex: 19 à 23), modifiés uniquement par la stat du personnage liée à cet élément
- Colonnes définitives validées (différentes du CSV source d'origine) :
  `Nom du sort` · `Description` · `Coût en PA` · `Portée min` · `Portée max` · `Portée modifiable` · `Ligne de vue requise` · `Zone d'effet` · `Lancers par tour` · `Lancers par combat` · `Lancers par cible` · `Portée diagonale/ligne` · `Intervalle de relance (CD)` · `Durée de l'effet` · `Cumul des effets` · `Rang d'évolution` · `Dégâts min` · `Dégâts max` · `Élément` · `Dégâts critique min` · `Dégâts critique max` · `Chance de critique`
  (le CSV `DEDALE SORTS.csv` a des colonnes légèrement différentes — se référer à la liste ci-dessus comme version définitive)

## Logique de calcul de dégâts (validée)

Chaque stat d'un cube est liée à un élément (ex: Intelligence → Feu, Agilité → Air). Cette stat ne booste que les dégâts des sorts du même élément.

Exemple : un sort Terre à 20 dégâts de base tape toujours 20 s'il n'y a pas de stat Terre équipée. Un sort Feu à 20 dégâts de base peut taper 60 si le joueur a de l'Intelligence équipée.

**Taux de conversion stat → % de dégâts** : jugé simple par le porteur de projet, pas encore formalisé, à écrire noir sur blanc au moment du développement du module de calcul (Tâche 3).

Le calcul se décompose en **deux fonctions pures**, sans dépendance à Express ni MySQL, testables unitairement (`server/logic/calcul.js`) :

```js
// Étape 1 : agréger les stats du personnage à partir des cubes équipés
calculerStatsPersonnage(cubesEquipes) → { intelligence, agilite, force, chance, vitalite, ... }

// Étape 2 : calculer les dégâts de chaque sort à partir des stats du personnage
calculerDegats(statsPersonnage, sortsEquipes) → { sortId, degatsMin, degatsMax }[]
```

## Stats dérivées et bonus de panoplie (validés, implémentés dans `calculerStatsPersonnage`)

Au-delà des stats brutes sommées telles quelles depuis les cubes, certaines stats affichées sur la fiche personnage se calculent différemment :

- **Vitalité** = base **1050** + somme des bonus Vitalité des cubes
- **PA** = base **7** + cubes + bonus panoplie
- **PM** = base **3** + cubes + bonus panoplie
- **Invocation** = base **1** + cubes
- **PO**, **Sagesse**, **Soins**, **Puissance**, toutes les **Résistances**, **Dommages critique** (`DO_CRIT`), **Dommages poussée** (`DO_POU`) : base 0, simple somme des cubes (déjà géré génériquement, rien de spécial à coder)
- **Tacle** = 1 par tranche entière de 10 Agilité (troncature, pas d'arrondi)
- **Fuite** = 1 par tranche entière de 10 Chance (troncature) + bonus Fuite direct des cubes
- **Retrait PA / Retrait PM** = 1 par tranche entière de 10 Sagesse (troncature). Pas de stat cube équivalente ; augmenté par certaines **breloques** (onglet "Boosts breloques", branché au calcul — voir "État d'avancement").
- **Esquive PA / Esquive PM** = même palier Sagesse (10 → 1) **+ bonus direct des cubes** (stat `ESQUIVE_PA`/`ESQUIVE_PM`, confirmée présente sur certains cubes) — contrairement au Retrait, les cubes peuvent bien booster l'Esquive.
- **Dommages élémentaires affichés** (Terre/Eau/Feu/Air) = stat `DOMMAGES` (globale, jamais affichée seule) + `DO_<ELEMENT>` (propre à l'élément)
- **Dommages critique du sort** (`degats_critique_min`/`degats_critique_max` sur `Sort`, distinct de la stat cube `DO_CRIT`) : intégré à `calculerDegats` (Tâche 8) — même formule `ax+b` que les dégâts normaux, appliquée à la plage critique du sort. Affiché sur l'onglet Sorts de la fiche perso.
- **`DO_CRIT`** (stat cube, bonus de dommages qui ne s'applique que **si** un coup critique survient) : **toujours pas intégré** — `calculerDegats` calcule la plage de dégâts critiques du sort mais ne simule pas le jet de dé lui-même (survient ou non), donc ce bonus n'est pas encore ajouté au résultat. Valeur brute disponible dans les stats.
- **Dommages poussée** (`DO_POU`) : formule `(132 + DO_POU) / 4 × NombreDeCasesPoussées` — **pas implémenté**, le nombre de cases de poussée n'est pour l'instant que du texte libre dans les données sorts. Pas urgent (dixit porteur de projet).
- **Critique** (`%_COUP_CRITIQUE`) : base 0 + somme des cubes. Le % final d'un sort (`chanceCritiqueTotal`) = `chance_critique` du sort + `%_COUP_CRITIQUE` du personnage — intégré à `calculerDegats` (Tâche 8), affiché sur l'onglet Sorts.

### Bonus de panoplie

Équiper **au moins 2 cubes** d'une même famille (élément, ou Lumière) donne un bonus de stats. Le bonus au palier atteint **remplace** celui du palier précédent (pas cumulatif entre paliers). Plusieurs panoplies de familles différentes équipées en même temps se cumulent entre elles. Un cube **Chaos** compte comme 1 cube de **chaque** famille (Air/Feu/Terre/Eau/Lumière) pour le calcul des paliers, mais n'a pas de panoplie propre.

Valeurs connues à ce jour (config `PANOPLIES` dans `calcul.js`, facilement modifiable) :

| Cubes Lumière | Bonus |
|---|---|
| 2 | +1 PM, +50 Vitalité, +5 Soin, +5 DO_CRIT |
| 3 | +1 PM, +1 PA, +75 Vitalité, +10 Soin, +10 DO_CRIT |
| 4 | +1 PM, +1 PA, +1 PO, +100 Vitalité, +15 Soin, +15 DO_CRIT |
| 5 | +1 PM, +1 PA, +1 PO, +150 Vitalité, +20 Soin, +20 DO_CRIT |
| 6 | +2 PM, +1 PA, +1 PO, +175 Vitalité, +25 Soin, +25 DO_CRIT |
| 7 | +2 PM, +2 PA, +1 PO, +200 Vitalité, +30 Soin, +30 DO_CRIT |
| 8 | +2 PM, +2 PA, +2 PO, +225 Vitalité, +35 Soin, +35 DO_CRIT |
| 9 | +2 PM, +2 PA, +2 PO, +250 Vitalité, +40 Soin, +40 DO_CRIT |

| Cubes Air | Bonus |
|---|---|
| 2 | +50 Vita, +50 Agilité, +10 DO_AIR |
| 3 | +100 Vita, +100 Agilité, +20 DO_AIR |
| 4 | +150 Vita, +150 Agilité, +30 DO_AIR, +25 Puissance, +5 Dommages |
| 5 | +200 Vita, +150 Agilité, +30 DO_AIR, +50 Puissance, +10 Dommages |
| 6 | +250 Vita, +150 Agilité, +30 DO_AIR, +75 Puissance, +15 Dommages |
| 7 | +300 Vita, +150 Agilité, +30 DO_AIR, +100 Puissance, +20 Dommages |
| 8 | +350 Vita, +150 Agilité, +30 DO_AIR, +125 Puissance, +25 Dommages |
| 9 | +400 Vita, +150 Agilité, +30 DO_AIR, +150 Puissance, +30 Dommages |

**Terre, Eau, Feu** : mêmes valeurs que Air à l'identique sur tous les paliers (2-9), juste la caractéristique et la stat de dommages direct adaptées à l'élément (Terre→Force/DO_TERRE, Eau→Chance/DO_EAU, Feu→Intelligence/DO_FEU). **Confirmé par le porteur de projet (chantier "Ensembles" ci-dessous) : les 5 familles de panoplie de cubes (Lumière/Air/Terre/Eau/Feu), telles qu'actuellement codées dans `PANOPLIES`, sont les vraies valeurs du jeu — ce ne sont pas des placeholders, rien à corriger ici.** Un fichier `data/Liste-des-ensembles.md` fourni pour ce chantier contient bien une table de bonus pour les ensembles de cubes, mais avec des valeurs différentes et confirmées fausses par le porteur de projet — volontairement ignorée, `PANOPLIES` reste l'unique source de vérité pour ce groupe.

(Une version à une seule fonction `calculerDegats(cubesEquipes, sortsEquipes)` avait été proposée puis corrigée — l'étape intermédiaire d'agrégation des stats est centrale et doit rester séparée.)

## Modèle de données MySQL (schéma validé et implémenté)

**Nom de la base : `dedalofus`** (et non `dedale_book`, renommée en cours de Tâche 2).

⚠️ **`Cube` est un mot réservé en MySQL 8.0** (lié à `GROUP BY ... WITH CUBE`). Toute requête SQL qui référence cette table doit l'entourer de backticks : `` `Cube` ``. C'est déjà fait dans `schema.sql` et `import-cubes.js` — à reproduire dans tout futur code SQL touchant cette table (API Express, etc.).

```
Utilisateur
 - id, email, pseudo (UNIQUE, 3-32 caractères), mot_de_passe_hash, cree_le

Personnage
 - id, utilisateur_id (FK), nom, cree_le
 (pas de classe, pas de niveau)

Cube (référentiel, 420 entrées, lecture seule)
 - id, nom, element, rang, numero, image_url (nullable)

StatCube (une ligne par stat, car leur nombre varie selon le cube)
 - id, cube_id (FK), cle_stat (ex: FORCE, PUISSANCE, CHANCE, DO_AIR...), valeur, libelle

Breloque (référentiel)
 - id, nom, rang, effet
 (correspond exactement aux 3 colonnes du CSV)

Sort (référentiel — colonnes définitives listées ci-dessus)
 - id, nom, description, cout_pa, portee_min, portee_max, portee_modifiable,
   ligne_de_vue_requise, zone_effet, lancers_par_tour, lancers_par_combat,
   lancers_par_cible, portee_diagonale_ligne, intervalle_relance_cd,
   duree_effet, cumul_effets, rang_evolution, degats_min, degats_max,
   element, degats_critique_min, degats_critique_max, chance_critique

Breuvage (référentiel, 30 entrées : 10 familles x 3 rangs Petit/Normal/Grand)
 - id, nom, rang, cle_stat (ex: INTELLIGENCE, DO_CRIT, TACLE...), valeur_stat, image_url (nullable)

Equipement (= le "stuff" sauvegardé d'un personnage)
 - id, personnage_id (FK), lien_partage (identifiant unique pour partage public), mis_a_jour_le

EquipementCube (9 lignes par équipement)
 - equipement_id (FK), emplacement (1-9), cube_id (FK, nullable si vide)

EquipementBreloque (7 lignes par équipement)
 - equipement_id (FK), emplacement (1-7), breloque_id (FK, nullable si vide)

EquipementSort (9 lignes par équipement)
 - equipement_id (FK), emplacement (1-9), sort_id (FK, nullable si vide)

EquipementBreuvage (3 lignes par équipement)
 - equipement_id (FK), emplacement (1-3), breuvage_id (FK, nullable si vide)
```

Index utiles à prévoir au minimum : `element`, `rang`/`evolution` sur `Cube` (filtres).

## Format des données sources

- **Cubes** (`cubes_v2.json`) : tableau d'objets `{ id, nom, element, rang, numero, stats: [{ key, value, label }] }`. Le nombre de stats varie par cube (d'où la table `StatCube` séparée plutôt que des colonnes fixes).
- **Breloques** (`DEDALE BRELOQUES.csv`) : `Nom`, `Rang`, `Effet` — 116 lignes.
- **Sorts** (`DEDALE SORTS NOVICE DEDALE.csv`) : 115 lignes, colonnes à retraiter vers la liste définitive ci-dessus lors de l'import (pas un mapping 1:1 direct).

## Parcours utilisateur cible

**Sans compte** : les 3 types d'équipement (cubes, breloques, sorts) restent consultables librement avec recherche/filtres via le menu du header, sans compte nécessaire. La page d'accueil elle-même ne pousse plus que vers la création de compte — bouton unique "Créer mon équipement" (mis en valeur), pas de bouton "Voir les équipements" sur l'accueil (implémenté différemment de l'intention initiale de ce document, sur demande du porteur de projet).

**Avec compte** : connexion → fiche personnage avec emplacements vides (9 cubes, 7 breloques, 9 sorts) → clic sur un emplacement vide → liste filtrée par type → sélection → retour fiche personnage remplie → onglet "Sorts" affichant les dégâts calculés → sauvegarde automatique à chaque changement → partage via lien unique consultable sans compte → gestion (voir/modifier/supprimer) des stuffs enregistrés.

## Plan de développement (tâches séquentielles)

1. **Mise en place du projet** ✅ FAIT (Jour 1) — voir "État d'avancement" ci-dessous
2. **Tables MySQL + import cubes/breloques/sorts** ✅ FAIT (Jour 3) — voir "État d'avancement" ci-dessous
3. **Module de calcul (2 fonctions pures) + tests unitaires** ✅ FAIT (Jour 3) — voir "État d'avancement" ci-dessous
4. **API Express pour exposer les équipements** ✅ FAIT (Jour 3) — routes 100% en français, cohérent avec le reste du code
5. **Pages React liste + détail équipement** ✅ FAIT — voir "État d'avancement" ci-dessous
6. **Authentification (inscription/connexion, bcrypt, JWT)** ✅ FAIT — voir "État d'avancement" ci-dessous
7. **Création de personnage + emplacements d'équipement** ✅ FAIT — voir "État d'avancement" ci-dessous
8. **Branchement du calculateur sur la fiche perso (onglet Sorts)** ✅ FAIT — voir "État d'avancement" ci-dessous
9. **Sauvegarde automatique du stuff** ✅ FAIT (déjà acquise depuis la Tâche 7 — voir "État d'avancement" ci-dessous)
10. **Partage par lien unique** ✅ FAIT — voir "État d'avancement" ci-dessous
11. **Déploiement** ✅ FAIT — voir "État d'avancement" ci-dessous

Règles d'enchaînement :
- Tâches 1 à 4 : strictement séquentielles
- Tâche 5 (liste/détail) et tâche 6 (auth) : peuvent démarrer en parallèle, dépendent seulement de la tâche 4
- Tâches 7 à 10 : séquentielles entre elles
- Tâche 11 : préparable en amont (choix hébergeur), exécution en dernier

## État d'avancement

### ✅ Jour 1 — Tâche 1 terminée
- Mono-repo `Dedalofus/client` + `Dedalofus/server`
- Backend Express + cors + dotenv, route `GET /api/ping` → `{ message: "pong" }`, testée OK
- Frontend Vite (React/JS/ESLint), fetch vers `/api/ping`, "pong" affiché dans le navigateur → CORS fonctionnel confirmé
- Git/GitHub lié, premier commit fait : "Setup initial: backend Express + frontend React connectés (test /api/ping)"
- Reporté volontairement : sous-dossiers `routes/`/`controllers/`/`models/`/`config/` (Tâche 4), script `concurrently` (optionnel)

### ✅ Jour 3 — Tâche 2 terminée
- MySQL 8.0 installé en local (MySQL Installer, config "Development Computer", "Full", port 3306)
- Base `dedalofus` créée via `schema.sql` (10 tables : Utilisateur, Personnage, Cube, StatCube, Breloque, Sort, Equipement, EquipementCube, EquipementBreloque, EquipementSort)
- Bug corrigé : `Cube` est un mot réservé MySQL → backticks ajoutés dans `schema.sql` et `import-cubes.js` (voir section "Modèle de données" ci-dessus)
- `scripts/` déplacé à la racine → `server/scripts/` (partage le `package.json`/`node_modules` du serveur, cohérent avec le mono-repo)
- Dépendances `mysql2` et `csv-parse` ajoutées à `server/package.json`
- Noms de fichiers CSV corrigés dans les scripts d'import (`DEDALE - BRELOQUES.csv`, `DEDALE - SORTS.csv` — espaces, pas underscores)
- Les 3 imports exécutés avec succès : **420 cubes** (+ 1755 lignes StatCube), **116 breloques**, **115 sorts**
- Vérifié : `SELECT * FROM `Cube` WHERE element = 'Feu'` renvoie 75 résultats → critère "fini" de la Tâche 2 rempli
- Commit : "Ajout des tables MySQL et scripts d'import (cubes, breloques, sorts)"

### ✅ Jour 3 (suite) — Tâche 3 terminée
- `server/logic/calcul.js` créé avec les 2 fonctions pures `calculerStatsPersonnage` et `calculerDegats`
- Formule de conversion validée et implémentée : `dégâts = base × (1 + 0,01 × stat_efficace) + bonus_dommages`
  - `stat_efficace` (élément du sort) = caractéristique liée à l'élément (Force→Terre, Intelligence→Feu, Chance→Eau, Agilité→Air) **+ Puissance** (1 Puissance = 1 stat dans tous les éléments)
  - `bonus_dommages` = stat `DOMMAGES` (globale, tous éléments) + stat `DO_<ELEMENT>` (propre à l'élément, ex: `DO_FEU`)
  - Chaos et Lumière ne sont **pas** des éléments de frappe (juste des familles de cubes) ; les sorts Chaos/Lumière tapent soit dans le "meilleur élément" (le plus avantageux des 4 pour le perso), soit dans 2 éléments à la fois (2 calculs séparés), soit pas de dégâts
  - Arrondi à l'entier le plus proche **uniquement à l'affichage final** (jamais pendant le calcul, ni dans les calculs intermédiaires)
- `calculerStatsPersonnage` agrège génériquement toutes les stats brutes des cubes équipés (pas de liste figée), + calcule l'Initiative dérivée (Force + Intelligence + Chance + Agilité + bonus Initiative des cubes Lumière)
- Vitest installé en version **2.x** (la 4 nécessite Node 20+, incompatible avec le Node 18 installé) ; 15 tests unitaires écrits et tous verts, dont les 2 exemples chiffrés validés par le porteur de projet (250 stat + 11 dommages sur base 20 → 81 ; 450 Intel + 170 Puissance sur sort Feu 20-22 → 144 à 158)
- Bonus découvert au passage : `server/node_modules/` et `server/.env` étaient suivis par git depuis le tout premier commit (pas de `.gitignore` racine) → `.gitignore` créé, fichiers désinscrits du suivi (sans suppression locale)
- Commit : "Ajout du module de calcul de degats (Tache 3) + gitignore"

### ✅ Jour 3 (suite 2) — Stats dérivées + bonus de panoplie ajoutés
- Toutes les formules de stats dérivées (Vitalité, PA, PM, Invocation, Tacle, Fuite, Retrait/Esquive PA/PM, Dommages élémentaires affichés) intégrées dans `calculerStatsPersonnage` — détail complet dans la section "Stats dérivées et bonus de panoplie" ci-dessus
- Bonus de panoplie implémentés (config `PANOPLIES` dans `calcul.js`, facilement éditable), avec gestion du comptage des cubes Chaos (comptent comme 1 cube de chaque famille)
- Esquive PA/PM confirmée boostable directement par certains cubes (stat `ESQUIVE_PA`/`ESQUIVE_PM`), en plus du palier Sagesse — contrairement au Retrait PA/PM qui n'a pas d'équivalent cube
- 29 tests unitaires au total, tous verts
- Commit : "Ajout des stats derivees et bonus de panoplie au calcul (Tache 3)", pushé sur GitHub

### ✅ Jour 3 (suite 3) — Tâche 4 terminée
- Nommage des routes tranché : **100% français** (`/api/cubes`, `/api/breloques`, `/api/sorts`), pas d'anglais, cohérent avec le reste du code
- `server/config/db.js` : pool de connexions MySQL (`mysql2/promise`), réutilisé par tous les contrôleurs
- `server/controllers/` : `cubesController.js`, `breloquesController.js`, `sortsController.js` — logique des routes
- `server/routes/` : `cubes.js`, `breloques.js`, `sorts.js` — déclaration des routes Express
- Routes implémentées :
  - `GET /api/cubes` — filtres `nom` (recherche partielle), `element`, `rang`, pagination `limit`/`offset` (max 500)
  - `GET /api/cubes/:id` — détail d'un cube avec ses stats jointes (format identique à `cubes_v2.json` : `{ ...cube, stats: [{ key, value, label }] }`), 404 si introuvable
  - `GET /api/breloques` — filtres `nom`, `rang`, pagination
  - `GET /api/sorts` — filtres `nom`, `element`, `rang` (mappé sur `rang_evolution`), pagination
- `server/index.js` monte les 3 routers + middleware d'erreur global (Express 5 route automatiquement les rejets de promesses des handlers async, pas besoin de try/catch dans les contrôleurs)
- Testé en conditions réelles avec `curl` : filtres, détail, 404, recherche — tout fonctionne
- Bug de session résolu : une ancienne instance du serveur (lancée plus tôt) squattait le port 3001 et masquait les nouvelles routes → tuée avant de retester

### ✅ Tâche 5 terminée — Pages React liste/détail + filtres avancés
- `react-router-dom` ajouté ; Vite downgradé 8→5 et `@vitejs/plugin-react` 6→4 côté client (même incompatibilité Node 18 que Vitest, cf. Tâche 3)
- Thème sombre créé (fond `#4A433B`, choisi par le porteur de projet façon DofusBook), couleur dédiée par élément, logo intégré (`client/src/assets/logo.webp`)
- Cartes `CubeCard`/`BreloqueCard`/`SortCard` suivant la maquette fournie (entête + image/placeholder à gauche + stats à droite, format stat affiché `valeur libellé` ex: "6% Résistance Feu", espace prévu pour une future icône par stat)
- 3 pages liste (Cubes/Breloques/Sorts) + détail Cube, toutes consultables sans compte, navigation commune dans le header
- Page d'accueil : uniquement **"Créer mon équipement"** (gros bouton mis en valeur) si déconnecté ; **+ "Voir mes équipements"** si connecté (les deux pointent vers `/personnage`, placeholder Tâche 7) — le bouton "Voir les équipements" a été retiré de l'accueil (les 3 listes restent accessibles via le header)
- Bug corrigé : le champ `nom` des cubes vaut toujours littéralement "Cube" (aucune valeur distinctive) → la recherche libre porte aussi sur `element`, `rang` et `numero` côté API
- Filtres : cubes = élément + rang (sélection unique, reclique pour décocher) ; breloques + sorts = **multi-sélection** (plusieurs valeurs actives à la fois, OR entre elles) ; sorts ont aussi un filtre rang de maîtrise (Novice/Expert/Maître α/Maître ẞ, liste partagée avec les breloques via `rangsMaitrise.js`)
- Filtres avancés cubes : bouton "+ de filtres" (transition CSS `grid-template-rows` 0fr→1fr, fluide sans hauteur fixe), une case à cocher par stat **réellement présente** sur au moins un cube (35 stats vérifiées via `SELECT DISTINCT` en base, `client/src/constants/statsCubes.js`), clic sur le texte ou la case fonctionne (label HTML natif), **combinées en ET** (le cube doit avoir toutes les stats cochées, pas une seule)
- **Décision : mobile-first à partir de maintenant** (Tâche 7 incluse) pour tout nouveau code — les pages déjà construites ci-dessus n'ont pas été retouchées, aucune media query pour l'instant, passe dédiée à faire plus tard (voir "Points encore en suspens")
- Catégories de filtres breloques (Dégâts/Mobilité/Soin.../Boss) demandées mais **pas encore décidées** par le porteur de projet, reportées à plus tard (plus de breloques à venir)

### ✅ Tâche 6 terminée — Authentification
- `bcryptjs` (plutôt que `bcrypt`, pas de compilation native à gérer sous Windows) + `jsonwebtoken`, token valide 30 jours
- Routes `POST /api/auth/inscription` et `POST /api/auth/connexion` ; middleware `verifierToken.js` prêt pour protéger les routes personnage/stuff (Tâche 7), pas encore utilisé
- Colonne `pseudo` ajoutée à `Utilisateur` (UNIQUE, 3 à 32 caractères) — un joueur ne doit pas être identifié juste par son email ; le header affiche le pseudo (pas l'email) une fois connecté
- Anti-doublon : vérification email+pseudo avant insertion, **et** filet de sécurité contre une race condition (contrainte `UNIQUE` en base + catch propre de l'erreur `ER_DUP_ENTRY`, réponse 409 au lieu d'une 500)
- Regex email stricte (norme HTML5, domaine avec point obligatoire) pour rejeter les faux emails type `0@0`, qui passaient auparavant sans aucune validation de format
- `AuthContext` React : session (token + utilisateur) persistée dans `localStorage`, survit aux rafraîchissements
- Redirections : un utilisateur déjà connecté qui visite `/connexion` ou `/inscription` est redirigé automatiquement (plus de formulaire réaffiché inutilement)
- Google OAuth demandé puis **repoussé** après plus tard : complexité de config externe (Google Cloud, redirect URI) non justifiée pour le MVP d'un site bénévole
- Repo GitHub : ancienne branche `master` (vieux prototype statique sans rapport, un seul commit) renommée en `archive` ; remote local mis à jour vers la nouvelle URL `pataupe/Dedalofus` (le repo avait été renommé côté GitHub, ancien nom `Dedale-Book`)

### ✅ Tâche 7 terminée — Personnage, grille d'équipements, flux "Équiper"
- **Décision de découpage** : Tâche 7 attaquée en 2 bouts plutôt que d'un coup.
- **Nouveau 4e type d'équipement décidé** : les **Breuvages** (3 emplacements), en plus des cubes/breloques/sorts — voir section "Les 4 types d'équipements" ci-dessus. Toujours pas de data ni de table : 3 cases statiques non cliquables côté frontend, rien côté backend.
- **Bout 1** : `POST`/`GET /api/personnages` (création + liste, `nom` seul), protégées par `verifierToken` (premier branchement réel du middleware) ; `PersonnagePage.jsx` remplace le placeholder.
- **Bout 2** — la vraie grille d'équipement :
  - `creerPersonnage` crée maintenant aussi l'`Equipement` associé + ses 25 lignes d'emplacements vides (9 `EquipementCube` + 9 `EquipementSort` + 7 `EquipementBreloque`, en bulk insert mysql2 `VALUES ?`) ; `lien_partage` généré via `crypto.randomBytes` dès la création (colonne `NOT NULL UNIQUE`, même si le partage lui-même n'est pas encore utilisé, Tâche 10)
  - `GET /api/personnages/:id` renvoie la fiche complète (nom + les 25 emplacements avec l'item posé ou `null`), `PUT /api/personnages/:id/{cubes,sorts,breloques}/:emplacement` équipe un item (une seule requête `UPDATE ... JOIN Equipement`, pas de round-trip supplémentaire pour retrouver `equipement_id`)
  - **Flux "Équiper" retenu** (à réutiliser pour tout futur type d'emplacement) : cliquer un emplacement **vide** sur la fiche perso navigue vers la vraie page liste du type concerné (`/cubes?perso=<id>&emplacement=<n>`, etc.) — pas de vue "picker" dédiée, on réutilise les pages liste existantes avec tous leurs filtres. Ces 3 pages liste détectent les query params `perso`/`emplacement` et affichent un bouton "Équiper" par carte (visible seulement si connecté) ; le clic appelle la route `PUT` puis redirige vers `/personnage/:id`.
  - Nouvelle page `PersonnageDetailPage.jsx` (route `/personnage/:id`) : grille de 28 cases affichées à l'écran (les 25 réelles + les 3 breuvages statiques), nouveau composant partagé `EmplacementSlot.jsx` (case vide cliquable pointillée vs. case remplie, réutilisé pour les 4 types plutôt que de dupliquer la logique 28 fois)
  - Après création d'un personnage, redirection automatique vers sa fiche (`/personnage/:id`) au lieu de rester sur la liste
  - **Explicitement hors scope, repoussé à la Tâche 8** : les stats calculées du personnage (affichage sous la grille) et l'onglet "Sorts" avec le calculateur de dégâts — les deux ont besoin de pouvoir déjà équiper des cubes/sorts, donc logiquement après ce bout
- **Bout 3** — polish visuel de la grille + modale de détail (retouches demandées après un premier retour visuel du porteur de projet, maquettes `MaquetteEquipementVide.jpg` et `maquetteCube.png`) :
  - Layout corrigé : cubes (3×3) + sorts (3×3) + colonne des 3 breuvages côte à côte (le CSS des breuvages utilisait par erreur une grille à 3 colonnes au lieu d'une colonne à 3 lignes — repositionnés), breloques en rangée en dessous, tout centré et agrandi (slots 64px mobile / 80px desktop, contre 56px avant)
  - `EmplacementSlot.jsx` : plus de "+" affiché dans les cases vides (juste un style de case creuse, bordure qui réagit au survol) ; les cases **remplies** sont maintenant aussi cliquables (avant : affichage seul)
  - Cases cube équipé affichent `élément + numéro` (ex: "Air 3") plutôt que `nom` (qui vaut toujours littéralement "Cube", cf. bug connu Tâche 5) — seul affichage utile en attendant les vraies images (V1 sans images, cf. "Sourcing des images" ci-dessous)
  - **Clic sur une case remplie → modale de détail**, nouveau composant générique réutilisable `Modal.jsx` (fond cliquable pour fermer, touche Échap, bouton ×) : réutilise directement `CubeCard`/`SortCard`/`BreloqueCard` (déjà construits Tâche 5) comme contenu, pas de nouveau composant de détail à écrire
    - Sorts et breloques : la fiche perso (`GET /api/personnages/:id`) a été élargie pour inclure les colonnes manquantes (`cout_pa`/`description` pour les sorts, `effet` pour les breloques) — tout est déjà là, la modale s'ouvre instantanément
    - Cubes : les stats ne sont **pas** incluses dans la fiche perso (évite de les charger pour 9 cubes à chaque chargement de page) — récupérées à la demande via l'endpoint existant `GET /api/cubes/:id` au clic, modale ouverte immédiatement puis mise à jour dès que les stats arrivent
  - Breuvages : toujours aucune donnée/table, cases vides non cliquables inchangées — la demande de modale ne s'applique pas encore à ce type
- **Bout 4** — bordures colorées par rang sur les cases équipées (indicateur visuel en attendant les vraies images) :
  - Cubes (`client/src/constants/rangs.js`, `couleurRangCube`) : Commun = bronze, Rare = argent, Épique = or, Mythique = rouge écarlate, Éxalté = diamant (bleu glacé `#CFEFFF` + léger halo lumineux via `box-shadow`, pensé pour rester visuellement distinct du bleu-vert de l'élément Air `#7fd1c8`)
  - Breloques et sorts (`client/src/constants/rangsMaitrise.js`, `couleurRangMaitrise`, échelle de rangs partagée) : Novice = bordure par défaut (rien de spécial), Expert = argent, Maître α/ẞ = or
  - `s.rang_evolution` ajouté à la requête sorts de `GET /api/personnages/:id` (manquant jusqu'ici, nécessaire pour calculer la couleur de bordure)
  - `EmplacementSlot.jsx` : nouvelles props `bordure`/`lueur`, bordure des cases remplies passée de 1px à 3px pour que la couleur de rang soit bien visible
- **Bout 5** — contrainte "un seul exemplaire par cube" : un même cube (élément + numéro, ex: "Air 4") ne peut être équipé qu'à un seul emplacement à la fois, **même à un rang différent** (Air 4 Commun et Air 4 Mythique s'excluent mutuellement ; Air 4 et Feu 4 restent compatibles). Vérifié dans `equiperCube` (désormais une fonction autonome, plus besoin du helper générique `equiper` partagé avec sorts/breloques qui n'ont pas cette contrainte) : requête de conflit sur `EquipementCube JOIN Cube` scoped au personnage, `409 { erreur }` avec le nom de l'emplacement en conflit si trouvé. Message backend propagé tel quel jusqu'à l'utilisateur (`CubeListPage.jsx` affiche `err.message` au lieu d'un message générique).
- **Bout 6** — déséquiper un item (aucune route backend à ajouter : `PUT .../:emplacement` acceptait déjà un id `null` pour vider l'emplacement, il manquait juste le déclencheur côté frontend) :
  - Petite croix dans le coin haut-droit de chaque case remplie (`EmplacementSlot.jsx`, nouvelle prop `onDesequiper`, `stopPropagation` pour ne pas déclencher aussi l'ouverture de la modale) : déséquipement direct en un clic, sans passer par la modale
  - Bouton rouge "Déséquiper" ajouté dans la modale de détail (les 3 types : cube/sort/breloque)
  - `PersonnageDetailPage.jsx` : mise à jour de l'état local après un déséquipement réussi (pas de refetch complet de la fiche), fermeture automatique de la modale si l'item déséquipé y était affiché
- **Bout 7** — petites retouches UX suite aux retours du porteur de projet :
  - `Modal.jsx` : le scroll vertical est désormais isolé dans un conteneur interne (`.modale__corps`) séparé du conteneur externe qui porte le bouton × en débordement négatif — évitait une barre de défilement horizontale parasite (le débordement du bouton faisait passer `overflow-x` à `auto` sur tout le conteneur)
  - `EmplacementSlot.css` : la petite croix de déséquipement n'est visible qu'au survol/focus de la case (`opacity` 0→1 via `:hover`/`:focus-within`), plus jamais affichée en permanence
  - `PersonnagePage.css` : le lien de chaque personnage dans "Mes personnages" est maintenant `display: block` sur toute la zone du `<li>` (avant : seul le texte était cliquable, le padding autour ne réagissait pas)
  - `Header.css` : pseudo agrandi (17px, gras, couleur accent dorée `--couleur-lumiere`) pour plus de présence une fois connecté ; bouton "Déconnexion" recoloré en rouge (`--couleur-feu`, même couleur que "Déséquiper") plutôt que neutre, cohérent avec les autres actions de sortie/suppression de l'appli
  - `PersonnageDetailPage.jsx` : lien "← Retour à mes personnages" ajouté en haut de la fiche, vers `/personnage`
- **Bout 8** — flux "Équiper" sans redirection automatique : cliquer "Équiper" sur `CubeListPage`/`BreloqueListPage`/`SortListPage` ne navigue plus tout de suite vers la fiche perso. À la place, nouveau composant partagé `Toast.jsx` (notification discrète en bas d'écran, transition slide up/down via une classe `visible` togglée en CSS, `position: fixed`) qui propose un lien "Voir ma fiche" et se ferme seule après 3s (`setTimeout` stocké dans un `useRef`, remis à zéro à chaque nouvel équipement pour ne pas empiler plusieurs toasts — un seul toast affiché à la fois, son minuteur redémarre). Permet d'équiper plusieurs items d'affilée depuis la même page liste sans revenir sur la fiche à chaque fois.
  - **Bug découvert juste après** : rester sur la page liste et cliquer "Équiper" sur plusieurs cubes/sorts/breloques différents ré-équipait toujours le **même** emplacement (celui figé dans l'URL au moment du clic sur la case vide d'origine, `?perso=<id>&emplacement=<n>`) — un seul item au final au lieu de plusieurs. Corrigé en changeant le contrat de la route : `PUT /api/personnages/:id/{cubes,sorts,breloques}` (sans `:emplacement` dans l'URL) choisit désormais **automatiquement le premier emplacement libre** du bon type côté serveur (`trouverEmplacementLibre`, `SELECT ... WHERE <colonne>_id IS NULL ORDER BY emplacement LIMIT 1`) ; la contrainte "un seul exemplaire par cube" (Bout 5) reste vérifiée avant de chercher un emplacement libre. La route `PUT .../:emplacement` (avec emplacement précis) reste utilisée telle quelle, mais uniquement pour le **déséquipement** depuis la fiche perso, qui a besoin de cibler une case précise. Les liens des cases vides sur `PersonnageDetailPage.jsx` n'ont donc plus besoin du paramètre `emplacement` (juste `?perso=<id>`).

### 🔄 Tâche 8 (bout 1 fait) — Stats calculées sous la grille
- Le module `server/logic/calcul.js` (Tâche 3) n'était jusqu'ici appelé que par ses tests unitaires — jamais exposé par une route. `obtenirPersonnage` (`GET /api/personnages/:id`) calcule maintenant réellement `calculerStatsPersonnage` à partir des cubes équipés (stats batch-fetchées avec le même pattern `StatCube WHERE cube_id IN (...)` que `cubesController.listerCubes`) et ajoute le résultat dans la réponse (`{ ..., stats }`)
- Nouveau composant `client/src/components/StatsPersonnage.jsx`, affiché sous la grille d'équipement sur `PersonnageDetailPage.jsx` : 5 blocs façon DofusBook (maquettes fournies par le porteur de projet) — Principal (Vitalité/PA/PM/PO/Invocation/Initiative/% Critique/Soin), Caractéristiques, Mobilité (Fuite/Tacle/Esquive/Retrait), Dommages, Résistances — toutes les stats affichées même à 0, config de libellés en dur dans le composant (les clés dérivées de `calcul.js` mélangent `_TOTAL`/`_TOTALE`, à respecter exactement)
- **Pas de recalcul optimiste côté client** : les stats sont recalculées côté serveur à chaque `GET /api/personnages/:id`, donc un rafraîchissement de page suffit après avoir équipé/déséquipé un cube — pas de logique de calcul dupliquée en JS frontend
- **Bout 2** — bonus de panoplie affichés sur la fiche perso :
  - **Terre/Eau/Feu ajoutés à `PANOPLIES`** (`calcul.js`) : jusqu'ici seuls Lumière et Air avaient des valeurs. En l'absence de vraies données pour ces 3 familles, mêmes valeurs que Air à l'identique sur tous les paliers (2 à 9), juste la caractéristique et la stat de dommages direct adaptées (Terre→FORCE/DO_TERRE, Eau→CHANCE/DO_EAU, Feu→INTELLIGENCE/DO_FEU) — entièrement **fictif**, à corriger dès que le porteur de projet fournit les vraies valeurs. Les 5 familles ont donc maintenant une table de bonus.
  - Nouvelle fonction exportée `calculerPanopliesActives(cubesEquipes)` (`calcul.js`) : renvoie le détail **par famille** (`{ famille, nombre, palier, bonus }[]`) des ensembles actifs, sans les fusionner — contrairement à `calculerBonusPanoplies` (utilisée pour le calcul des stats), qui reste inchangée mais est maintenant réécrite pour réutiliser `calculerPanopliesActives` en interne (pas de logique dupliquée). 4 nouveaux tests unitaires (33 au total, tous verts).
  - `GET /api/personnages/:id` renvoie ce détail dans `{ ..., panoplies }`
  - Nouveau composant `client/src/components/PanopliesPersonnage.jsx`, affiché sous `StatsPersonnage` : n'affiche qu'**un seul ensemble à la fois** ("Ensemble de Cubes Air (7)" + ses stats), pour éviter une page à rallonge quand plusieurs familles sont actives en même temps (ex: cubes Chaos comptant dans les 5 familles à la fois → jusqu'à 5 ensembles actifs simultanément). Cliquer sur le titre ouvre un menu déroulant listant les autres ensembles actifs ; en choisir un bascule l'affichage dessus. Ensemble par défaut = celui avec le plus de cubes comptés (Chaos inclus), départagé par un ordre fixe en cas d'égalité. Si un seul ensemble est actif, le titre redevient un simple texte non cliquable (pas de menu vide à ouvrir pour rien).
- **Bout 3** — réorganisation Caractéristiques + colonne "Parcho" (maquette DofusBook fournie par le porteur de projet) :
  - Bloc Principal : la ligne `VITALITE_TOTALE` s'appelle maintenant "PdV" (au lieu de "Vitalité") pour ne pas être confondue avec la nouvelle ligne "Vitalité" du bloc Caractéristiques (la stat `VITALITE` brute des cubes seule, sans le +1050 de base)
  - Bloc Caractéristiques réordonné : Vitalité, Sagesse, Force, Intelligence, Chance, Agilité, puis Puissance à part (Puissance n'a **pas** de Parcho, confirmé sur la capture)
  - **"Parcho"** : bonus de caractéristiques éditable par le joueur (façon scrolls), **persisté en base** (décision du porteur de projet, comme l'équipement) — 6 colonnes ajoutées à `Equipement` (`parcho_vitalite`, `parcho_sagesse`, `parcho_force`, `parcho_intelligence`, `parcho_chance`, `parcho_agilite`, toutes `INT NOT NULL DEFAULT 0`), migrées par script Node ponctuel (pas de client `mysql` en CLI sur la machine) et répercutées dans `schema.sql`
  - `GET /api/personnages/:id` renvoie `parcho: { VITALITE, SAGESSE, FORCE, INTELLIGENCE, CHANCE, AGILITE }` ; nouvelle route `PUT /api/personnages/:id/parcho` (`sauvegarderParcho`) sauvegarde les 6 valeurs à la fois (entiers ≥ 0)
  - `StatsPersonnage.jsx` : chaque ligne Caractéristiques (sauf Puissance) a maintenant une case éditable, total affiché = stat des cubes + Parcho ; 3 boutons "0"/"100"/"150" sous la colonne remplissent les 6 cases à la fois et sauvegardent immédiatement ; une case modifiée à la main sauvegarde au `onBlur`
  - **Bug corrigé pendant le dev** : l'`onBlur` lisait `parchoLocal` (state React) au lieu de la valeur réelle du champ, capturée par une closure qui pouvait être en retard d'un render sur le `onChange` — corrigé en relisant `e.target.value` directement dans le handler `onBlur` plutôt que de faire confiance au state
- **Bout 4** — le Parcho ne remontait pas dans les stats dérivées (PdV, Tacle, Fuite, Retrait/Esquive PA-PM, Initiative) : il ne servait qu'à l'affichage brut des 6 lignes Caractéristiques, sans influencer le reste du calcul. Corrigé à la source : `calculerStatsPersonnage(cubesEquipes, bonusParcho)` (`calcul.js`) prend maintenant un 2e paramètre optionnel, fusionné dans les stats brutes **avant** toute dérivation — exactement comme les bonus de panoplie, donc tout ce qui dépend de VITALITE/SAGESSE/FORCE/INTELLIGENCE/CHANCE/AGILITE en profite automatiquement, y compris l'Initiative (effet de bord logique, pas juste les stats explicitement demandées). 5 nouveaux tests unitaires (38 au total). `obtenirPersonnage` passe maintenant `parcho` en 2e argument. Le "dynamique" demandé reste **côté serveur** (pas de duplication des formules en JS) : `StatsPersonnage.jsx` republie la fiche perso (`onParchoSauvegarde`, callback vers `rafraichir()` dans `PersonnageDetailPage.jsx`) juste après chaque sauvegarde Parcho réussie (au blur ou au clic sur 0/100/150) — mise à jour quasi instantanée sans recharger la page, mais pas à chaque frappe.
- **Bout 5** — retouche du menu déroulant des panoplies (maquette Dédale fournie par le porteur de projet) :
  - `PanopliesPersonnage.jsx`/`.css` : le déclencheur ressemble maintenant à un vrai sélecteur (bordure, fond distinct, bouton flèche coloré `--couleur-lumiere` à droite qui pivote à l'ouverture), précédé d'un petit label "Bonus de panoplie :"
  - **Bug de positionnement corrigé** : le menu déroulant est maintenant dans son propre conteneur `position: relative` (`.panoplies-personnage__selecteur`), séparé du reste du bloc — avant, il était positionné par rapport à toute la carte (titre + stats), donc `top: 100%` le plaçait sous les stats au lieu de sous le titre. Il s'ouvre maintenant juste sous le déclencheur et recouvre les stats en dessous (`position: absolute` + `z-index`), sans repousser la mise en page.
- **Bout 6** — 45 sorts utilitaires masqués du site : sorts sans dégâts ni élément (ex: "Botte - Novice", "Aimantation - Novice" — nom se terminant par "- Novice", pas à confondre avec la colonne `rang_evolution`), hors sujet pour un calculateur de dégâts. Décision : ne pas les supprimer, juste les masquer — nouvelle colonne `Sort.visible` (`TINYINT(1) NOT NULL DEFAULT 1`, migrée par script Node ponctuel comme pour Parcho, et ajoutée à `schema.sql`), mise à `0` pour ces 45 lignes. `sortsController.listerSorts` filtre désormais toujours `WHERE visible = 1` (en plus des filtres existants) — 70 sorts visibles au lieu de 115. Filtre appliqué uniquement à la liste/recherche : la fiche perso (`obtenirPersonnage`) n'y touche pas, un sort déjà équipé continuerait de s'afficher normalement même s'il devenait invisible.
- **Bout 7** — dégâts manquants récupérés depuis la colonne "Effet du sort" du CSV source : certains sorts (poisons, pièges, glyphes, invocations — ex. "Poison Insideux") n'ont rien dans "Dégâts de base"/"Dégâts critique", mais la valeur est écrite en texte libre dans "Effet du sort"/"Effet du sort (Critique)" (ex: `"16 à 18 (dommages Feu) (2 tours)"`). Nouvelle fonction `parseDegatsDepuisEffet` (`server/scripts/import-sorts.js`) : découpe le texte sur `/` et prend le premier segment qui ressemble à des dégâts, **en ignorant les segments de soin** (`"PV rendus"` — un sort de soin comme "Mot Soignant" contient aussi une plage "X à Y" mais ce n'est pas des dégâts, à ne pas confondre). Utilisée comme repli uniquement quand la colonne dédiée est vide.
  - Corrigé dans `import-sorts.js` pour les futurs imports à partir de zéro, **et** appliqué à la base actuelle via un script `UPDATE` ponctuel ciblé par `nom` (pas de ré-import complet `DELETE`+`INSERT`, qui aurait cassé les références `EquipementSort` existantes et réinitialisé la colonne `visible` tout juste renseignée) : 15 sorts corrigés en dégâts de base, 5 en dégâts critiques.
  - `SortCard.css` : `flex: 1` ajouté sur `.carte-sort` — les cartes de la page Sorts n'avaient pas toutes la même hauteur dans une même rangée de la grille (le conteneur `.page-sorts__carte`, qui porte aussi le bouton "Équiper", s'étirait bien à la hauteur de ligne, mais pas la carte à l'intérieur).
- **Bout 8** — `SortCard.jsx` complétée (maquette DofusBook fournie, comparée champ par champ) : Portée (min-max), % de critique (`chance_critique`), Lancers par tour, Lancers par cible, Dégâts critiques (`degats_critique_min/max`) ajoutés, tout en `null`-safe (masqué si absent, ex. les sorts sans dégâts n'affichent que PA/Portée/lancers). Champs de la maquette **volontairement pas repris** : Niveau, sélecteur de rang de sort (1-6), bouton "Explication du sort", ID — aucun ne s'applique à notre modèle (pas de niveaux/rangs de sort au Dédale). Le libellé "Allié/Ennemi" (type de cible) est aussi ignoré : pas de colonne correspondante en base, non prioritaire.
  - **Décision de découpage** : cette carte a en réalité 2 affichages prévus par le porteur de projet — une version "liste" (hors équipement, dégâts et % critique **de base**, faite ici) et une version "onglet Sorts" (dégâts et % critique **calculés** pour le personnage, via `calculerDegats` — jamais branché, dépend de l'onglet Sorts de la Tâche 8 qui reste à construire). Seule la version liste est faite dans ce bout.
- **Bout 9** — mise en page de `SortCard.jsx` revue (le premier jet empilait tout en une seule colonne, façon liste brute, alors que la maquette DofusBook sépare visuellement les dégâts des autres stats) :
  - Dégâts normaux et critiques désormais dans un bandeau `.carte-sort__degats` à 2 blocs côte à côte (séparateur vertical entre les deux), valeur en gros/gras, libellé en petit en dessous ; dégâts critiques teintés `--couleur-feu` pour les distinguer visuellement du reste
  - Le reste des stats (PA, Portée, % critique, Lancers par tour/cible) en grille 2 colonnes sous un séparateur, avec la même convention de pastille d'icône réservée que `CubeCard` (`carte-sort__stat-icone`, vide en attendant les vraies icônes)

### ✅ Tâche 8 terminée — Onglet "Sorts" avec calculateur de dégâts
- `calculerDegats` (`server/logic/calcul.js`, Tâche 3, jamais branché jusqu'ici) étendu de façon rétrocompatible : accepte en plus, par sort, `degatsCritiqueMin`/`degatsCritiqueMax`/`chanceCritique` (optionnels) ; applique la même formule `ax+b` aux dégâts critiques qu'aux dégâts normaux, et calcule `chanceCritiqueTotal` = % critique du sort + `%_COUP_CRITIQUE` du personnage. Ces clés n'apparaissent dans le résultat que si les données sources existent (aucune régression sur les tests existants). 4 nouveaux tests unitaires (42 au total, tous verts).
- `obtenirPersonnage` (`personnagesController.js`) : la requête sorts passe à `SELECT es.emplacement, s.*` (au lieu d'énumérer les colonnes une par une, qui oubliait `degats_critique_min/max`/`chance_critique`/`portee_*`/`lancers_par_*` — nécessaires pour que `SortCard` s'affiche correctement sur l'onglet Sorts). Les sorts équipés sont passés à `calculerDegats`, le résultat exposé dans `{ ..., degats }`.
- `SortCard.jsx` : nouvelle prop optionnelle `calcul` (résultat de `calculerDegats` pour ce sort) — quand fournie, remplace les dégâts/élément/% critique de base par les valeurs calculées ; PA/Portée/Lancers restent toujours ceux du sort (pas liés au calcul). Sans cette prop (page `/sorts` publique), comportement inchangé. Une seule carte pour les deux affichages plutôt qu'une carte dupliquée.
- Nouveau composant `client/src/components/OngletSorts.jsx` : grille de `SortCard` (avec `calcul`) pour chaque sort équipé ; un sort à 2 éléments (cas géré par `calculerDegats` mais pas encore rencontré dans les vraies données) produirait 2 cartes, une par élément.
- `PersonnageDetailPage.jsx` : deux onglets "Équipement"/"Sorts" (state `ongletActif`, boutons façon filtres existants) — "Équipement" = grille + `StatsPersonnage` + `PanopliesPersonnage` (contenu inchangé, juste déplacé dans une branche de condition) ; "Sorts" = `OngletSorts`. La modale de détail reste commune aux deux onglets.

### ✅ Polish visuel post-Tâche 8 — icônes stats/panoplies, header mobile, mise en page Principal/Caractéristiques
- `StatsPersonnage.jsx`/`PanopliesPersonnage.jsx` : chaque stat affiche désormais une petite icône emoji colorée (couleur d'élément existante, pas de vraie icône en V1) + libellé abrégé façon DofusBook (ex: "Do Feu", "Ré Terre", "Esq. PA") pour tenir sur 2 colonnes même en portrait mobile ; chaque bloc a un bandeau de titre coloré (même convention que l'en-tête `SortCard`). `ICONES_STATS` (Panoplies) couvre les 35 stats de `STATS_CUBES` avec un repli générique si une stat imprévue apparaît dans un bonus.
- **Alignement des icônes corrigé** (retour DofusBook comparé) : `.stats-personnage__valeur`/`.panoplies-personnage__valeur` ont une largeur fixe + `text-align: right` — la valeur s'étend vers la gauche selon son nombre de chiffres, l'icône reste toujours à la même position (avant : l'icône se décalait vers la droite avec des valeurs à plus de chiffres).
- Bloc "Principal" : mise en page à 2 colonnes **fixes** (`PRINCIPAL_GAUCHE`/`PRINCIPAL_DROITE`, pas l'auto-répartition des autres blocs) — PdV/PA/PM/PO à gauche, Invoc./Init./Crit. %/Soin à droite. PdV a une prop `vedette` (légèrement plus grand, 18px vs 15px) pour ressortir un peu des 7 autres stats du bloc, sans être criard.
- Bloc "Caractéristiques" : l'en-tête "Total" était mal centré (flottait entre les 2 colonnes) → aligné à gauche au-dessus des valeurs. Les boutons de remplissage Parcho 0/100/150 sont maintenant petits et poussés à droite (< la moitié de la largeur sur mobile), ce qui a permis de remonter la ligne Puissance sur la même ligne qu'eux (elle était isolée à tort) ; le séparateur entre cette ligne et les Caractéristiques au-dessus a été retiré.
- **Bonus de panoplie "Cumuler les bonus"** : nouvelle option dans le menu déroulant (`PanopliesPersonnage.jsx`), visible dès que ≥ 2 familles sont actives — additionne stat par stat les bonus de toutes les familles actives (`cumulerBonus`), plutôt que d'afficher une seule famille à la fois. Depuis cette vue, le menu propose de rebasculer vers n'importe quelle famille individuelle.
- `Header.jsx`/`.css` refondu pour mobile : menu hamburger (3 barres → croix animée) qui déroule un panneau vertical sous le point de rupture 720px (corrige au passage un débordement horizontal de 16px qui existait déjà sur la nav) ; header rendu `sticky`. Lien "Personnage" renommé **"Mes stuffs"** et distingué visuellement des 3 listes consultables librement (Cubes/Breloques/Sorts en pastille neutre grise, "Mes stuffs" en accent doré — même traitement que le bouton Connexion, car c'est le seul lien qui mène au compte du joueur).

### ✅ Tâche 9 (déjà acquise) — Sauvegarde automatique du stuff
- Aucun développement supplémentaire nécessaire : chaque action (équiper/déséquiper un cube/sort/breloque, sauvegarder le Parcho) persiste déjà immédiatement en base via son propre appel API (voir Tâche 7/8) — pas de bouton "Sauvegarder" à ajouter, pas d'état non synchronisé côté client à gérer.

### ✅ Tâche 10 terminée — Partage par lien unique
- `lien_partage` (généré à la création du personnage depuis la Tâche 7, jamais exploité jusqu'ici) est maintenant renvoyé par `GET /api/personnages/:id` (`{ ..., lienPartage }`), pour que le propriétaire puisse le récupérer/copier depuis sa fiche.
- `personnagesController.js` refactorisé : la construction de la fiche complète (stats, panoplies, équipement, dégâts) est extraite dans `construireFichePersonnage(personnage)`, partagée entre `obtenirPersonnage` (privée, vérifie `utilisateur_id`) et la nouvelle `obtenirPersonnagePartage` (publique, vérifie `lien_partage`) — pas de logique dupliquée entre les deux routes.
- Nouvelle route **publique** (sans `verifierToken`) `GET /api/partage/:lienPartage`, montée depuis un routeur dédié `server/routes/partage.js` (`/api/partage`) plutôt que dans `routes/personnages.js`, pour qu'aucun middleware d'auth ne s'y applique par erreur. 404 générique si le lien est invalide.
- `PersonnageDetailPage.jsx` : bouton "🔗 Copier le lien de partage" sous le nom du personnage (`navigator.clipboard.writeText`, feedback "Lien copié !" pendant 2s ; message d'erreur si le presse-papier est refusé par le navigateur).
- Nouvelle page publique `client/src/pages/PartagePage.jsx` (route `/partage/:lienPartage`, sans compte nécessaire) : réutilise la même grille d'équipement + `StatsPersonnage` + `PanopliesPersonnage` + `OngletSorts`, mais **en lecture seule** — pas de lien "Équiper" sur les cases vides (pas de prop `lien`), pas de croix de déséquipement (pas de prop `onDesequiper`), pas de bouton "Déséquiper" dans la modale de détail. Les cases remplies restent cliquables pour voir le détail de l'item (modale commune, réutilise `CubeCard`/`SortCard`/`BreloqueCard` comme sur la fiche privée).
- `StatsPersonnage.jsx` : nouvelle prop `lectureSeule` — quand `true`, la colonne Parcho s'affiche en texte simple (pas d'`<input>`) et les boutons 0/100/150 sont masqués ; `token`/`personnageId`/`onParchoSauvegarde` deviennent inutiles dans ce mode (un visiteur ne doit pas pouvoir modifier le stuff de quelqu'un d'autre).

### ✅ Tâche 11 terminée — Déploiement
- **Nom de domaine** : `dedalofus.fr` acheté chez OVHcloud.
- **Hébergeur** : VPS OVH (gamme VPS-1, 2 vCores/4 Go RAM/40 Go SSD, datacenter Strasbourg), payé au mois sans engagement — largement suffisant pour le trafic attendu (dizaines d'utilisateurs). IP : `164.132.198.133`.
- **Serveur** : Ubuntu 26.04, Node.js 22 (LTS), MySQL 8.4 — pas de contrainte de version dans le projet, choisi indépendamment du Node 18 utilisé en local (cf. section Vulnérabilités ci-dessous, propre au poste de dev).
- **Process serveur** : géré par **PM2** (`pm2 start index.js --name dedalofus-api`), configuré pour redémarrer automatiquement au boot du VPS (`pm2 startup` + `pm2 save`) et en cas de crash.
- **Nginx** en reverse proxy : sert les fichiers statiques du build React (`client/dist`) et route `/api/*` vers Node en local (port 3001). Le port 3001 n'est **pas** exposé publiquement (bloqué par le pare-feu `ufw`, seuls 22/80/443 sont ouverts) — tout passe obligatoirement par Nginx.
- **HTTPS** : certificat Let's Encrypt via Certbot (plugin nginx), renouvellement automatique programmé, redirection HTTP→HTTPS automatique.
- **SSH** : authentification par clé uniquement (mot de passe désactivé côté serveur après mise en place de la clé), pour limiter les tentatives de brute-force sur le port 22 exposé publiquement.
- **Secrets de prod** : `server/.env` du VPS a son **propre** mot de passe MySQL et son propre `JWT_SECRET`, générés directement sur le serveur (jamais saisis dans une commande locale) — aucun secret partagé avec l'environnement de dev local.
- **Base de données** : recréée à partir de `schema.sql` + les 3 scripts d'import (420 cubes, 116 breloques, 115 sorts). Point notable : le masquage des 45 sorts "- Novice" (`Sort.visible = 0`, Tâche 8 Bout 6) est une correction **ponctuelle** faite uniquement sur la base locale à l'époque, jamais répercutée dans `import-sorts.js` — a dû être réappliquée manuellement après l'import sur le VPS (`UPDATE Sort SET visible = 0 WHERE nom LIKE '%- Novice'`). ⚠️ Si la base est un jour réimportée de zéro (VPS ou autre), ne pas oublier cette étape — le script d'import ne la fait pas tout seul.
- **DNS** : zone DNS `dedalofus.fr` chez OVH — enregistrements `A` et `AAAA` (IPv4 et IPv6) pointés vers le VPS pour `@` et `www`. Les autres enregistrements (MX/SRV/CNAME liés aux emails OVH, NS, SPF, TXT) n'ont pas été touchés.
- Parcours complet testé en conditions réelles sur `https://dedalofus.fr` après déploiement (inscription, création de personnage, équipement d'un cube, lien de partage public) — toutes les données de test ensuite supprimées de la base de prod.
- **Procédure de redéploiement** (mise à jour du code déjà en prod, pas une réinstallation) : `git pull` sur le VPS, `npm run build` côté client si le front a changé, réappliquer les permissions `chmod o+x`/`o+r` sur `client/dist` (Vite vide et recrée ce dossier à chaque build), puis `pm2 restart dedalofus-api` si le backend a changé. Une trentaine de secondes, rien à voir avec l'installation initiale.

### ✅ Page "Monstres du Dédale" — contenu statique, sans compte ni base de données
- **Nouvelle section du site, explicitement repoussée après le MVP d'origine** (voir "Périmètre du MVP" ci-dessus, qui l'excluait) — ajoutée après la Tâche 11 (déploiement), documentée ici après coup (trou de documentation repéré en répondant à une question du porteur de projet sur la fraîcheur de ce fichier).
- **Aucune table MySQL, aucune route API** : contenu purement statique (pas de compte, pas de relation avec un personnage), donc juste un fichier JSON importé directement par le frontend React — cohérent avec la priorité "vitesse de sortie du MVP" du projet, décision explicite du porteur de projet consignée dans le script lui-même.
- **Pipeline de génération** : `data/monstres.md` (507 lignes, Markdown écrit à la main par le porteur de projet, hiérarchie de titres stricte : `# Profondeur` → `## Biome :` → `### Famille` → `#### Monstre` ou `#### BOSS : Monstre`) → `server/scripts/generate-monstres.js` (exécuté manuellement, pas branché sur un script npm ni le déploiement) → `client/src/data/monstres.json`. Le générateur :
  - Découpe le Markdown en profondeurs/biomes/familles/monstres via une petite machine à états ligne par ligne (`parserMonstresMd`), texte libre entre le titre de famille et le premier monstre = `passif` partagé de la famille.
  - Découpe le texte libre sous chaque monstre en paragraphes (séparés par ligne vide), typés `texte`/`mecanique`/`conseils` selon que le paragraphe commence littéralement par "Mécanique :"/"Conseils :" (réservé aux boss, stratégies plus détaillées).
  - Associe une image à chaque monstre par correspondance de nom assouplie (accents retirés, suffixe de dimensions `-WIDTHxHEIGHT` ignoré) parmi les fichiers de `data/images/monstres/`, copiée vers `client/public/images/monstres/<slug>.webp` — **84/84 monstres illustrés**, aucun manquant.
  - Slugs générés par monstre (kebab-case, désaccentué, doublons dédupliqués avec un suffixe `-2`) pour les URLs `/monstre/:slug`.
  - **Résultat actuel** : 4 profondeurs, 7 biomes, 14 familles (6 avec un passif partagé), 84 monstres dont 14 boss.
- **Frontend** :
  - `MonstreListPage.jsx` (`/monstres`) : tout l'état de sélection (profondeur/famille/monstre affiché) vit dans la **query string de l'URL** (`useSearchParams`) plutôt qu'en `useState` local — permet au menu du header ou à un lien de partage de sauter directement sur une profondeur/un monstre précis, et rend l'URL elle-même partageable/rechargeable sans perte de sélection. Sélecteur de profondeur en style "bouton enfoncé" (même convention que les onglets de la fiche perso, différent exprès des filtres pastille des pages Cubes/Breloques/Sorts, car c'est la navigation principale de la page). Filtre famille en sélection unique (reclic pour désactiver), affiché seulement si la profondeur en a plus d'une.
  - Clic sur une carte → modale (`Modal.jsx`, nouvelle variante `large`) contenant `MonstreDetail.jsx` : image, passif de famille en accordéon **replié par défaut** (technique `grid-template-rows` 0fr→1fr déjà utilisée ailleurs), déclencheur avec son propre fond/bordure + texte "Afficher"/"Masquer" explicite (une première version avec juste une flèche ne signalait pas assez que c'était cliquable, corrigé ensuite) ; paragraphes Mécanique (bordure violette/`--accent`)/Conseils (bordure rouge/`--couleur-feu`) toujours dépliés, réservés aux boss.
  - `MonstreCard.jsx` : image (`object-fit: contain`, pas `cover` — les portraits ont des ratios très variés, du quasi-carré au portrait très haut type Hululord/Brabuzar, `cover` rognait le bas des plus hautes) ou placeholder losange, badge BOSS, premier paragraphe `texte` en extrait.
  - Grille desktop (≥900px) forcée à **3 colonnes fixes** (au lieu de `auto-fill`, qui pouvait donner 5-6 colonnes et une dernière ligne quasi vide pour les petites familles) + cartes basculées en layout horizontal (image ~30% à gauche) pour réduire leur hauteur sans changer la largeur de grille.
  - `MonstrePartagePage.jsx` (`/monstre/:slug`) : pas une vraie page de détail, juste un redirecteur client — cherche le slug dans `monstresData` et fait un `<Navigate>` vers `/monstres?profondeur=<cle>&monstre=<slug>` (réutilise l'état piloté par l'URL de `MonstreListPage`), ou vers `/monstres` seul si le slug est introuvable.
  - Bouton "🔗 Copier le lien de cette fiche" : `navigator.clipboard` seul plantait silencieusement (avant même d'atteindre le `.then()`) sur certains navigateurs mobiles, donnant l'impression que le bouton ne faisait rien — repli sur `document.execCommand('copy')` via un `<textarea>` invisible si l'API moderne est absente/bloquée.
- **Backend** — `server/routes/partageMonstre.js` (montée sur `/monstre`, même fichier `server/index.js` que `partageOg.js`) : même principe de détection de bot (`Discordbot`/`Twitterbot`/etc., même regex `BOT_UA`) que le partage de stuff, **mais plus simple** — un monstre a déjà une vraie image toute faite (le portrait des cartes), pas besoin d'en générer une à la volée comme `ogImage.js` pour les stuffs : `og:image` pointe directement dessus, pas de dimensions ni de segment `:version` anti-cache (contenu statique, jamais besoin de forcer un aperçu neuf). Description de l'aperçu = tous les paragraphes du monstre concaténés (pas juste le premier comme dans une version antérieure), tronqués à 1000 caractères. Un `Map` slug → monstre construit une fois au démarrage du serveur (pas de requête MySQL, cohérent avec l'absence de table).
- **Bug corrigé plus tard** (voir plus haut, "Fix carte monstre géante sur desktop") : la hauteur `auto` de l'image sur desktop faisait exploser la hauteur de la carte pour un portrait très haut/étroit (ex: Lazuli) — repassée à une hauteur fixe.

### ✅ Post-MVP — UX de remplacement d'équipement, avertissement inscription, footer
- **Erreurs d'équipement en popup** : les erreurs (cube déjà équipé ailleurs, etc.) s'affichent désormais via le composant `Toast.jsx` (généralisé pour accepter `texte`/`erreur`, plus seulement le message de succès figé "Équipé !"), au lieu d'un `<p>` en haut de page facilement raté. Variante visuelle rouge (`toast--erreur`) pour les distinguer des succès.
- **Nouveau code d'erreur backend `COMPLET`** (`equiperCubeAuto`/`equiperAuto` dans `personnagesController.js`) quand tous les emplacements d'un type sont déjà occupés, propagé jusqu'au front via `err.code` (`appelPersonnages` dans `client/src/api/personnages.js` attache désormais `code` à l'`Error` levée, pas seulement le message).
- **`ModaleRemplacement.jsx`** (nouveau composant partagé) : déclenchée quand `equiper()` reçoit `COMPLET` depuis une page liste (Cubes/Breloques/Sorts) alors que les 9/9/7 emplacements sont pleins — liste les emplacements actuels (réutilise `EmplacementSlot`) avec le texte "Choisissez le [cube/sort/breloque] à remplacer", clic = équipe sur l'emplacement précis choisi (route `PUT .../:emplacement`, pas la route auto).
- **Bouton "Remplacer"** dans la modale de détail d'un item équipé (`PersonnageDetailPage.jsx`, à côté de "Déséquiper") : lien vers la page liste correspondante avec **`emplacement` explicite dans l'URL** (`?perso=<id>&emplacement=<n>`), contrairement au flux normal d'équipement (Tâche 7 Bout 8) qui ne passe plus ce paramètre exprès (auto-sélection du premier emplacement libre, pour équiper plusieurs items d'affilée sans conflit). Les 3 pages liste détectent ce paramètre `emplacement` : si présent, elles équipent directement sur cet emplacement précis (pas la route auto, pas de risque de remplir un autre slot libre à la place) et le bouton affiche "Remplacer" au lieu de "Équiper".
- **Avertissement sur `InscriptionPage.jsx`** : bloc visuel (bordure/fond rouge) avant le formulaire, prévenant explicitement de ne pas réutiliser les identifiants du compte Dofus Touch — site non affilié à Ankama.
- **Nouveau composant `Footer.jsx`**, affiché sur toutes les pages (monté une fois dans `App.jsx`, après les `Routes`) : mention de non-affiliation à Ankama Games (Dofus Touch = marque déposée), liens vers Cubes/Breloques/Sorts, lien vers le serveur Discord du porteur de projet. Pas encore de lien CGU/mentions légales ni de contact dédié — en attente d'infos du porteur de projet (identité à afficher pour les mentions légales, obligatoires en France même pour un site non-commercial).
- **Mise en page "footer collant"** (`App.jsx`/`index.css`) : wrapper `.app-mise-en-page` (flex column, `min-height:100vh`) + `.app-contenu` (`flex:1`) autour des `Routes`, pour que le footer reste toujours en bas de l'écran sur les pages courtes au lieu de laisser un grand vide en dessous.
- **Page `/mentions-legales`** (`MentionsLegalesPage.jsx`, liée depuis le footer) : hébergeur (OVH, infos vérifiées via recherche web), disclaimer Ankama, section données personnelles/RGPD, cookies, limitation de responsabilité. L'éditeur reste **anonyme vis-à-vis du public** (droit prévu par l'article 6-III de la LCEN pour les particuliers non professionnels, l'identité réelle étant déjà connue de l'hébergeur via le compte OVH) — aucun nom ni email personnel affiché ; le contact RGPD/éditeur redirige vers le Discord du projet. Crédit "Site développé par Pataupe" dans le footer.

### ✅ Premières images réelles (breloques + sorts, rang Novice)
- Le porteur de projet a fourni les premières images du jeu (icônes façon plaque en bois), une par breloque/sort au rang **Novice** uniquement pour l'instant (Expert/Maître α/ẞ et les autres types — cubes, breuvages — viendront plus tard).
- **Colonne `image_url`** ajoutée à `Breloque` et `Sort` (comme `Cube` l'avait déjà depuis le début) — `schema.sql` mis à jour, migration appliquée en local puis en prod.
- **Fichiers sources** dans `data/images/{cubes,breloques,sorts,breuvages}/` (même convention que les CSV/JSON existants), nommés clairement par le porteur de projet (ex: `breloque_du_rixeur_instable_novice.webp`, `sort_poison_insidieux_feu_novice.webp`). **Copiés** (pas déplacés) vers `client/public/images/{breloques,sorts}/<id>.webp` (id de la ligne en base) — c'est ce second dossier qui est réellement servi par Vite/Nginx et référencé par `image_url` en base (`/images/breloques/<id>.webp`).
- **Script de correspondance automatique** (ponctuel, pas commité — logique à reproduire si de nouvelles images arrivent) : convertit chaque nom de fichier en un "sac de mots significatifs" (accents retirés, minuscules, mots vides comme "de/du/l/d" ignorés) et le compare au `nom` de chaque ligne en base (pour les sorts, le nom stocké inclut déjà l'élément en suffixe, ex: "Poison Insidieux - Feu" → il faut le retirer avant de comparer ; les sorts Chaos/Lumière sont stockés avec `element = "Meilleur élément"`, pas littéralement "Chaos"/"Lumière", d'où une clé de repli sans élément).
- **Une breloque a une seule image partagée entre ses 4 rangs** (Novice/Expert/Maître α/ẞ) : l'icône ne change pas selon le rang, seuls les chiffres de l'effet changent — l'image Novice fournie est donc appliquée aux 4 lignes de même `nom` en base.
- **Résultat** : 116/116 breloques (tous rangs confondus, les 29 noms existants matchent tous) et 66/115 sorts ont désormais une image. Les images restantes sans correspondance sont soit de nouveaux sorts/breloques pas encore en base (le porteur de projet a prévenu que le Dédale a du contenu qu'on n'a pas encore importé), soit sans image fournie pour l'instant.
- **3 coquilles corrigées en base** au passage (confirmées par le porteur de projet comme venant de la base, pas des images) : `Breloque de l'Embourgeur embourbé` → `Embourbeur`, `Poison Insideux` → `Insidieux`, `Ronce Agressives` → `Ronces Agressives` (au pluriel). Pas de script de migration dédié écrit pour ça (3 `UPDATE ... WHERE nom LIKE` ponctuels, appliqués en local puis en prod).
- `CubeCard.jsx` affichait déjà une image conditionnelle (`image_url` ? `<img>` : placeholder) depuis la Tâche 5 — **même pattern repris** pour `BreloqueCard.jsx` (nouveau bloc `carte-breloque__corps` avec image à gauche + effet à droite, la carte n'avait pas de zone image avant) et `SortCard.jsx` (voir bout suivant pour la taille de l'image, retravaillée juste après).
- **En attente du porteur de projet (annoncé sous ~2 jours)** : les données complètes (stats/effets/dégâts) des nouveaux sorts et breloques repérés via les images sans correspondance — une image seule ne suffit pas à créer une ligne exploitable en base.
- **Bout suivant** — retours visuels du porteur de projet après un premier aperçu des images en prod :
  - **`BreloqueCard.css`** avait le même bug de hauteur inégale entre cartes d'une même rangée que `SortCard` avant elle (Tâche 8 Bout 9) — même correctif (`flex: 1` sur `.carte-breloque`).
  - **Les images ne s'affichaient que sur les pages liste**, pas sur la fiche perso : `EmplacementSlot.jsx` accepte désormais une prop `image` (affiche l'`<img>` à la place du texte `libelle` si fournie) — branchée dans `PersonnageDetailPage.jsx` et `PartagePage.jsx` pour les cubes/sorts/breloques équipés (`cube.image_url`/`sort.image_url`/`breloque.image_url`, déjà présents dans la réponse API depuis les bouts précédents).
  - **Image de `SortCard.jsx` agrandie** : déplacée de la petite icône du bandeau d'en-tête (22px, à côté du nom) vers un vrai bloc image de 56px à gauche du corps de la carte (nouveau `.carte-sort__image`, même convention que `CubeCard`/`BreloqueCard` — image à gauche, contenu à droite dans une nouvelle div `.carte-sort__contenu` qui regroupe dégâts/stats/description).

### ✅ Deux bugs d'images de sorts corrigés + import des sorts Expert/Maître α/ẞ
- **Bug 1 — "Épée de Iop"** : le fichier source (`sort_epee_de_lop_..._novice.webp`) contenait une coquille ("de Lop" au lieu de "de Iop"), donc le script de correspondance par sac de mots (voir "Premières images réelles" ci-dessus) n'a jamais matché les deux lignes `Sort` correspondantes (id 77 et 78). Aucun souci côté base, juste une image jamais rattachée.
- **Bug 2 — "Bluff"** : ce sort a 2 lignes en base avec le même `nom` littéral ("Bluff"), distinguées uniquement par leur `element` ("Feu, Terre" / "Air, Eau" — l'élément n'est pas suffixé dans le nom comme pour les autres sorts à 2 versions). Le script de correspondance, pensé pour un nom unique par sort, n'a pas su départager les 2 fichiers images (`sort_bluff_feu_terre_novice.webp` / `sort_bluff_air_eau_novice.webp`) et n'a rien associé du tout aux 2 lignes (id 110 et 111).
- Corrigé manuellement : les 4 fichiers copiés vers `client/public/images/sorts/{77,78,110,111}.webp`, `image_url` mis à jour en base pour ces 4 lignes.
- **Import des rangs Expert / Maître α / Maître ẞ** : le porteur de projet a fourni 3 nouveaux CSV (`data/DEDALE - SORTS EXPERT - DEDALE.csv`, `data/DEDALE - SORTS MAÎTRE α - DEDALE.csv`, `data/DEDALE - SORTS MAÎTRE ẞ - DEDALE.csv`), plus une version resauvegardée du CSV Novice (`data/DEDALE - SORTS NOVICE.csv`, contenu strictement identique à l'ancien `data/DEDALE - SORTS.csv`, hors fins de ligne — `import-sorts.js` mis à jour pour pointer vers ce nouveau nom de fichier).
  - **Alignement des CSV vérifié avant tout import** : les 3 nouveaux fichiers contiennent exactement les mêmes 115 sorts que le CSV Novice, **dans le même ordre** (la ligne de données n°N correspond toujours au sort Novice d'id N en base) — mais avec des coquilles dans la colonne "Nom du sort" à 3 endroits (suffixe d'élément manquant sur "Piège de Masse"/"Piège d'entaille"/"Pulsar" ; virgule parasite dans "Invocation, de Yukisamara"). D'où le choix de faire correspondre les lignes **par position** plutôt que par nom.
  - Nouveau script **`server/scripts/import-sorts-rangs-superieurs.js`** (ne fait que des `INSERT`, jamais de `DELETE` — contrairement à `import-sorts.js` qui vide toute la table `Sort` à chaque exécution, ce qui aurait cassé les références `EquipementSort` existantes et les corrections ponctuelles déjà appliquées sur les sorts Novice) : pour chaque nouveau rang, associe la ligne CSV n°i au sort Novice d'id i+1 en base, reprend le `nom` du sort Novice (corrige au passage les coquilles ci-dessus, ainsi que les 3 fautes déjà connues type "Poison Insideux"/"Ronce Agressives"), réutilise les fonctions de parsing de `import-sorts.js` (dégâts, pourcentages...), et copie `image_url`/`visible` du sort Novice correspondant — **même image réutilisée sur les 4 rangs** (comme pour les breloques) en attendant de vraies images par rang, et même règle de masquage des sorts utilitaires (ex: "Botte - Novice" reste masqué à tous les rangs, le suffixe "- Novice" fait partie du nom du jeu, indépendant de `rang_evolution`).
  - Exécuté une fois : **345 sorts insérés** (115 × 3 rangs), aucune donnée Novice touchée. Testé en conditions réelles sur `/sorts` (filtre par rang, recherche "Iop"/"Bluff"/"Botte") : rangs supérieurs affichés avec les bonnes stats croissantes, images chargées en 200, sorts masqués toujours masqués.
  - Script pensé pour être **rejouable sans doublons** : si des sorts existent déjà pour un rang donné, l'import de ce rang est simplement sauté (log explicite).
  - ⚠️ **Procédure de ré-import complet à jour** : `schema.sql` → `import-cubes.js` / `import-breloques.js` / `import-sorts.js` (Novice) → **`import-sorts-rangs-superieurs.js`** (nouveau, à ne pas oublier) → réappliquer les corrections ponctuelles déjà documentées (masquage `visible = 0`, 3 coquilles de noms, cf. Tâche 11 et "Premières images réelles") → **suppression des sorts mono-rang** (point suivant).
- **Bug suivant, repéré juste après déploiement** : `SortCard.jsx` n'affichait jamais `rang_evolution`, contrairement à `BreloqueCard.jsx` qui affiche déjà "Nom — Rang" dans son en-tête (les breloques ont le même souci : même `nom` sur les 4 rangs). Résultat une fois les 3 nouveaux rangs en base : impossible de distinguer "Absorption Magique" (Novice) de ses 3 clones dans la liste. Corrigé en répliquant le même en-tête "Nom — Rang" sur `SortCard.jsx` (aucun changement en base, juste un oubli d'affichage).
- **Sorts mono-rang détectés et nettoyés** : en comparant automatiquement les 4 CSV colonne par colonne (dégâts, portée, % critique, effet...), **47 sorts sur 115 ont des stats strictement identiques sur les 4 rangs** — signe que le CSV les a dupliqués tels quels alors qu'ils n'ont pas de vraie évolution Expert/Maître en jeu (confirmé par le porteur de projet pour "Assaut Magique"/"Absorption Magique", les 2 seuls concernés parmi les sorts visibles ; les 45 autres sont les sorts utilitaires déjà masqués type "Botte - Novice", `visible = 0`, jamais vus mais dupliqués en base quand même). Script ponctuel (non commité, logique à reproduire si un nouveau CSV rangs supérieurs arrive un jour) : supprime les lignes `Expert`/`Maître α`/`Maître ẞ` de ces 47 sorts, ne garde que `Novice`. **141 lignes supprimées**, en local et en prod.

### ✅ Contrainte anti-doublon étendue aux sorts et breloques
- Signalé par le porteur de projet une fois les rangs supérieurs en base : rien n'empêchait d'équiper 2 fois le même sort ou la même breloque (ex: "Bluff" Novice **et** "Bluff" Expert en même temps), alors que les cubes ont déjà cette règle depuis la Tâche 7 Bout 5 (un même cube élément+numéro ne peut être équipé qu'une fois, rang différent inclus).
- Étendu le même principe aux sorts et breloques, mais avec **`nom`** comme clé d'unicité (au lieu de élément+numéro pour les cubes, qui n'a pas d'équivalent ici) — deux lignes de rang différent mais de même `nom` s'excluent mutuellement, peu importe le rang.
- `personnagesController.js` : nouvelle fonction partagée `trouverEmplacementDoublonParNom(personnageId, table, colonne, refTable, valeurId, emplacementAIgnorer)`, branchée dans `equiper()` (emplacement précis, `emplacementAIgnorer` = l'emplacement ciblé lui-même pour ne pas se bloquer en cas de remplacement) et `equiperAuto()` (premier emplacement libre). Les fonctions `equiperSort(Auto)`/`equiperBreloque(Auto)` passent maintenant un `refTable: 'Sort' | 'Breloque'` en plus de `table`/`colonne`/`type`. Aucun changement pour `equiperCube(Auto)`, qui garde sa propre logique élément+numéro (déjà correcte, `nom` de cube n'étant pas exploitable).
- Message d'erreur 409 accordé au genre du mot ("Ce sort est déjà équipé..." / "Cette breloque est déjà équipée...") plutôt qu'un message générique — remonté tel quel jusqu'au `Toast` d'erreur côté front (déjà en place depuis le Post-MVP, aucun changement frontend nécessaire).
- Testé via un personnage jetable (créé/supprimé par un script ponctuel non commité, pas de compte de test laissé en base) : équiper un sort puis sa version d'un autre rang → rejeté avec le bon emplacement en conflit, que ce soit via le flux auto ou le flux "Remplacer" (emplacement précis) ; ré-équiper le même item sur son propre emplacement ne se bloque pas lui-même ; un sort/breloque différent s'équipe normalement. Même vérification pour les breloques.

### ✅ Petites retouches — images dans la modale de remplacement + bordure des cases
- `ModaleRemplacement.jsx` ne passait pas `image_url` à `EmplacementSlot` (seule case affichant du texte au lieu de l'image, contrairement à la fiche perso/page de partage) — corrigé, même pattern repris.
- `EmplacementSlot.css` : bordure des cases équipées réduite de 5px à 4px (retouche visuelle demandée par le porteur de projet).

### ✅ Onglet "Boosts breloques" — bonus conditionnels paramétrables
- **Nouveau CSV fourni, `data/DEDALE - BRELOQUES_NEW.csv`** : refonte complète de la donnée breloques par le porteur de projet, avec 4 nouvelles colonnes de bonus conditionnel (`Bonus conditionnel minimum/maximum`, `incrément`, `Type d'input` [`toggle`/`range`/`bouton accumulateur`], `Bonus par défaut`) + `Tag` (catégorie, correspond aux boutons de filtre déjà présents mais inertes sur `/breloques`, "bientôt actifs") et `Ensemble lié` (regroupement en "sets" de breloques, façon panoplies de cubes — donnée stockée mais **non exploitée pour l'instant**, aucun bonus d'ensemble calculé).
- **Particularités du fichier découvertes à l'analyse** (avant tout code, comme demandé) :
  - Encodage **ISO-8859-1** (pas UTF-8), séparateur `;` — décodage explicite en `latin1` requis (`Buffer.toString('latin1')`), sinon accents corrompus.
  - Colonne `Rang` ne distingue **pas** Maître α de Maître ẞ : les deux valent littéralement la chaîne `"Maître ?"` (perte d'un caractère non supporté lors d'un export côté porteur de projet). Confirmé par le porteur de projet : **aucune hiérarchie entre α et ẞ** (juste un choix cosmétique du joueur), et dans ce CSV précis, l'ordre des 4 lignes d'une breloque est **toujours** `Expert, Maître α, Maître ẞ, Novice` (vérifié automatiquement sur les 52 groupes du fichier, 0 exception) — la position dans le fichier fait foi, pas le texte du `Rang`.
  - Coquilles/variantes de nom à fusionner avant import (comme pour les sorts) : "Forcene" → "Forcené", "Homeopathe" → "Homéopathe", "ballonne" → "ballonné", "Breloque **de** Dernier repli" → "Breloque **du** Dernier repli" (orthographe déjà en base) — sans cette normalisation, ces breloques auraient été dupliquées au lieu d'être mises à jour.
  - **27 breloques du CSV n'existaient nulle part en base** (jamais importées) : 23 après fusion des coquilles ci-dessus. Confirmé par le porteur de projet : leurs images étaient déjà fournies dans `data/images/breloques/` (9 breloques signalées en attente dans une session précédente, "Premières images réelles") — vérifié par correspondance automatique (sac de mots) : **11 des 23 nouvelles breloques ont une image**, les 14 autres (toutes de rang `Unique`, apparemment des breloques de boss) n'en ont pas encore.
  - Nouveau rang **`Unique`** introduit par ce CSV (14 breloques à une seule ligne, pas de déclinaison Novice/Expert/Maître) — pas ajouté à `RANGS_MAITRISE` (liste **partagée avec les sorts**, qui n'ont pas ce rang) : ces breloques restent filtrables par nom mais pas par le filtre de rang existant. `couleurRangMaitrise('Unique')` retombe simplement sur la bordure par défaut (fallback déjà en place, aucun changement de code nécessaire).
- **Schéma** (`schema.sql`) : colonnes ajoutées sur `Breloque` — `tag`, `ensemble_lie`, `type_input`, `bonus_min_texte`, `bonus_increment_texte`, `bonus_max_texte`, `bonus_defaut_texte` (texte brut du CSV conservé tel quel, ex: `"10 soins"`, `"dommages finaux x1.5"` — pas de parsing en base) ; et sur `EquipementBreloque` — `boost_valeur DECIMAL(10,3)` (valeur actuelle réglée par le joueur : 0/1 pour un toggle, la valeur numérique choisie pour range/accumulateur). Migration appliquée en local et en prod via script ponctuel non commité.
- **Nouveau module `server/logic/boosts.js`** (+ `boosts.test.js`, 21 tests) : logique pure, testée isolément comme `calcul.js`.
  - `parseValeurBoost(texte)` : extrait `{ valeur, prefixe, suffixe }` d'une chaîne brute — 2 formats gérés, "nombre puis unité" (`"10 soins"` → valeur 10, suffixe `" soins"`) et "multiplicateur de dommages" (`"dommages finaux distance x1.6"` → valeur 1.6, préfixe `"dommages finaux distance x"`). Un texte composite sans nombre en tête (ex: `"+20 Retrait PA + 20 Retrait PM"`, plusieurs stats à la fois sur un toggle) renvoie `valeur: null` — pas encore décomposé en effets structurés individuels (voir "Points encore en suspens").
  - `valeurBoostParDefaut(breloqueRow)` : calcule le `boost_valeur` initial à l'équipement, à partir de `bonus_defaut_texte` (pour un toggle, `"0"` = désactivé par défaut, tout autre texte = activé par défaut).
  - `clamperValeurBoost(breloqueRow, valeurBrute)` : borne une valeur envoyée par le joueur dans `[min, max]` (ou `0`/`1` pour un toggle) — utilisé par la route de sauvegarde, ne rejette jamais une valeur hors bornes, la ramène simplement dans la plage.
  - `construireBoost(breloqueRow, valeurActuelle)` : construit l'objet exposé par l'API pour une breloque équipée ayant un bonus conditionnel (`null` sinon).
- **`server/scripts/import-breloques-boosts.js`** (commité, rejouable) : lit le CSV, normalise les noms, regroupe par breloque et assigne les rangs par position (voir ci-dessus), puis pour chaque ligne fait un `UPDATE` si la breloque existe déjà (par `nom`+`rang`, ne touche jamais `image_url` des breloques déjà en base) ou un `INSERT` sinon (en tentant une correspondance d'image comme pour les sorts/breloques Novice). **Vérifié avant exécution réelle** avec un script de test à blanc : les 116 lignes existantes matchent bien en `UPDATE` (0 doublon accidentel), 50 lignes insérées pour les 23 breloques réellement nouvelles. Exécuté en local puis en prod : **116 breloques mises à jour, 50 insérées** (166 breloques au total, dont 83 avec un bonus conditionnel : 57 `toggle`, 23 `range`, 3 `accumulateur`).
- **Backend** (`personnagesController.js`) :
  - `construireFichePersonnage` : la requête breloques inclut maintenant les colonnes de bonus + `eb.boost_valeur`, et chaque breloque équipée expose `breloque.boost` (via `construireBoost`, `null` si pas de bonus conditionnel).
  - `equiper()`/`equiperAuto()` (génériques, déjà partagées entre sorts et breloques) : quand `refTable === 'Breloque'`, initialisent aussi `boost_valeur` à la valeur par défaut de la breloque équipée (`calculerValeurBoostInitiale`), en plus de poser `breloque_id` — une seule requête `UPDATE`, pas de round-trip supplémentaire.
  - Nouvelle route **`PUT /api/personnages/:id/breloques/:emplacement/boost`** (`sauvegarderBoostBreloque`) : modifie uniquement le bonus d'une breloque déjà équipée (pas besoin de la déséquiper/rééquiper), valeur bornée côté serveur via `clamperValeurBoost`.
- **Frontend** :
  - **3ᵉ onglet "Boosts breloques"** sur la fiche perso (`PersonnageDetailPage.jsx`), **entre** "Équipement" et "Sorts" — et sur `PartagePage.jsx` en lecture seule (nouvelle prop `lectureSeule` sur `OngletBoosts`, comme `StatsPersonnage`).
  - Nouveau composant **`OngletBoosts.jsx`** : une ligne par breloque équipée ayant `boost` non nul (image + nom + rang + effet + contrôle) ; message dédié si aucune breloque équipée n'a de bonus conditionnel. 3 sous-composants selon `boost.type` :
    - `ToggleBoost` : interrupteur on/off custom (pas la case à cocher native), couleurs du site (`--couleur-lumiere` = actif) — nouveau CSS dans `OngletBoosts.css`.
    - `RangeBoost` : `<input type="range">` natif (`accent-color` pour le thème), bornes/pas = `boost.min`/`boost.max`/`boost.increment`, valeur affichée à côté (préfixe/suffixe interpolés, ex: "dommages finaux x1.3").
    - `AccumulateurBoost` : stepper à boutons +/− (pas de slider), mêmes bornes/pas, boutons désactivés en butée.
  - Sauvegarde **immédiate** à chaque interaction (pas de bouton "Valider" séparé, contrairement au Parcho qui sauvegarde au blur) : `sauvegarderBoostBreloque` (nouvelle fonction `client/src/api/personnages.js`) appelée directement au clic/changement. **Depuis le branchement au calcul (voir "Boosts breloques branchés au calcul" ci-dessous), déclenche un refetch complet de la fiche** (`rafraichir`, comme `onParchoSauvegarde`) plutôt qu'une mise à jour locale optimiste — nécessaire pour que `personnage.degats` (recalculé côté serveur) reste à jour, même logique que le Parcho.
  - Testé de bout en bout (personnage/compte jetables) : les 3 types de contrôle (range/toggle/accumulateur) s'affichent avec la bonne valeur par défaut, se modifient et persistent après rechargement complet de la page, et s'affichent correctement en lecture seule sur la page de partage publique.

### ✅ Bug corrigé — débordement horizontal de la page perso (3 onglets)
- Signalé par le porteur de projet juste après la mise en prod de l'onglet Boosts, avec capture d'écran mobile à l'appui : les 3 boutons d'onglet (Équipement/Boosts breloques/Sorts) étaient anormalement écartés, "Équipement" apparaissant même coupé sur le bord gauche de l'écran — et le footer, plus bas sur la page, avait une marge vide parasite à droite.
- **Cause unique aux deux symptômes** : `.page-personnage-detail__onglets`/`.page-partage__onglets` avaient un `gap: 40px` et un padding de bouton fixes, jamais adaptés au mobile (seulement 2 boutons à l'origine, jamais retouché depuis). Avec le 3ᵃ bouton "Boosts breloques" (texte plus long), la rangée dépassait largement la largeur d'un écran mobile → tout le document devenait plus large que le viewport (aucun `overflow-x: hidden` de sécurité en place), d'où le décalage visible plus bas sur la page (le footer, lui correctement dimensionné, se retrouvait dans un document plus large que lui).
- Corrigé dans les deux fichiers CSS (`PersonnageDetailPage.css`/`PartagePage.css`) : `gap`/padding réduits sur mobile (10px / `9px 16px`), remontés à leur valeur d'origine (un peu réduite : 24px au lieu de 40px) seulement à partir de 600px, `flex-wrap: wrap` ajouté en filet de sécurité. Vérifié via mesure DOM (`scrollWidth === clientWidth`, plus de débordement) à 375px et en desktop.

### ✅ Filtres breloques branchés (catégories + nouveau rang "Boss")
- Les catégories de filtre existaient déjà dans l'UI (`CATEGORIES_BRELOQUES`, "bientôt actifs") mais n'envoyaient rien à l'API — désormais branchées sur `Breloque.tag` (colonne ajoutée par `DEDALE - BRELOQUES_NEW.csv`, voir "Onglet Boosts breloques").
- `breloquesController.listerBreloques` : nouveau paramètre `tags` (comma-separated, comme `rangs`), un `LIKE` par tag sélectionné combinés en OR — une breloque peut cumuler plusieurs tags dans une seule colonne texte (ex: `"Breloque boss + Dégâts + Entrave"`), le `LIKE` matche chaque tag indépendamment de sa position. `client/src/api/breloques.js`/`BreloqueListPage.jsx` : `categoriesActives` (déjà en state, purement visuel avant) passé tel quel en `tags`.
- **2 coquilles corrigées dans `CATEGORIES_BRELOQUES`** (`client/src/constants/categoriesBreloques.js`) pour matcher exactement les tokens réels en base (vérifiés via `SELECT DISTINCT tag`) : "Breloques boss" → "**Breloque boss**" (singulier — le pluriel ne matchait aucune ligne, `LIKE` sensible à ça même si la casse, elle, ne compte pas avec la collation par défaut MySQL) ; "Bonus divers" → "**Bonus Divers**" (D majuscule, cosmétique, la casse n'affecte pas le filtre mais autant refléter la vraie donnée).
- **Nouveau rang de filtre "Boss"**, demandé en plus de Novice/Expert/Maître α/ẞ — mappé sur le rang réel `Unique` (les 14 breloques ajoutées par le nouveau CSV, voir "Onglet Boosts breloques"). Pas ajouté à `RANGS_MAITRISE` (liste **partagée avec la page Sorts**, qui n'a pas ce rang) : nouvelle liste locale `RANGS_BRELOQUES` dans `BreloqueListPage.jsx` (`RANGS_MAITRISE` + `{ valeur: 'Unique', libelle: 'Boss' }`), libellé affiché ≠ valeur envoyée à l'API. Vérifié : le rang `Unique` et le tag `"Breloque boss"` couvrent exactement le même ensemble de 14 breloques (aucune des deux n'en contient une que l'autre n'a pas) — les deux filtres sont redondants par construction mais demandés par le porteur de projet à deux endroits différents de l'UI (rang = ligne principale, tag = "+ de filtres").
- Testé : chaque tag filtre le bon nombre de résultats (vérifié côté API avant tout test navigateur), le rang "Boss" renvoie les mêmes 14 breloques que le tag "Breloque boss", combinaison de plusieurs filtres à la fois vérifiée, aucun débordement horizontal introduit par le 5ᵉ bouton de rang (`.page-breloques__rangs` avait déjà `flex-wrap: wrap`, contrairement au bug des onglets ci-dessus).

### ✅ Boosts breloques branchés au calcul de dégâts
- Les bonus configurés dans l'onglet "Boosts breloques" (Tâche précédente, jusque-là purement affichage/sauvegarde) modifient désormais réellement les stats et les dégâts calculés — la vieille règle "effet de breloque purement informatif" (section "Les 4 types d'équipements" ci-dessus) est donc définitivement caduque pour toute breloque à bonus conditionnel.
- **Nouveau module `server/logic/effetsBreloques.js`** (+ `effetsBreloques.test.js`, 31 tests, fixtures reprises telles quelles depuis `SELECT DISTINCT nom, type_input, bonus_max_texte, bonus_defaut_texte FROM Breloque` pour coller à la vraie donnée) — interprète le **sens gameplay** d'un `boost` (celui déjà renvoyé par `construireBoost`, boosts.js, qui ne fait que du parsing d'affichage/bornes de slider) en 3 catégories d'effet, détectées par la forme du texte :
  1. **Multiplicateurs de dégâts** (`dommages finaux[ distance| mêlée] xN`) : `resoudreMultiplicateurs` gère nativement plusieurs occurrences dans un même texte (ex: Tireur d'élite, "distance x1.15, mêlée x0.8" en une seule chaîne). Cas particulier codé en dur : **Embrasement du Katragon** exprime son bonus en `"20%"` brut plutôt que la convention `"dommages finaux xN"` du reste du CSV (confirmé par sa description) — seule exception, un set de 1 nom.
  2. **Bonus simples** ("nombre + unité", ex: `"2 PA"`, `"10 soins"`, `"-100 Vitalité"`) : le suffixe (déjà extrait par `parseValeurBoost`) est mappé vers une clé de stat via une petite table (`SUFFIXE_VERS_STAT`) — couvre PA/PM/PO/Soin/Puissance/Vitalité/% Critique. Cas particulier : `"% PdV"` (Hémophile) route vers `pdvPourcent`, pas une stat plate (voir plus bas).
  3. **Textes composites** (>= 2 nombres signés dans le même texte, ex: Embourbeur "+20 Retrait PA + 20 Retrait PM ; - 10 Esquive PA - 10 Esquive PM", Forcené "-15% Coups Critiques, +40 Dommages Critiques") : motifs dédiés (`MOTIFS_COMPOSITES`) matchés globalement sur le texte entier, indépendamment de la ponctuation utilisée comme séparateur (très inconsistante dans le CSV — `;`, `,`, ou juste un signe répété).
  - Distinction 2 vs 3 faite en comptant les occurrences de `[+-]\d+` dans le texte (0 ou 1 → bonus simple ; >= 2 → composite) — évite de coder en dur un séparateur qui n'existe pas de façon fiable dans la donnée source.
  - `calculerEffetsBreloques(breloquesEquipees)` agrège l'effet de toutes les breloques équipées en un seul objet `{ statsPlates, pdvPourcent, multiplicateurs }`.
  - `combinerMultiplicateurs(multiplicateurs, modeAttaque)` applique **exactement** la règle donnée par le porteur de projet : même type de multiplicateur (`finaux`/`finaux_distance`/`finaux_melee`) → bonus additionnés (`x1.15 + x1.6 distance = x1.75`, pas `x1.84`) ; types différents → facteurs multipliés (`x1.55 finaux × x1.15 distance = x1.7825`) ; un multiplicateur `finaux_distance`/`finaux_melee` ne compte pas si `modeAttaque` ne correspond pas.
- **`calcul.js`** : `calculerStatsPersonnage(cubesEquipes, bonusParcho, effetsBreloques)` — 3ᵉ paramètre optionnel `{ statsPlates, pdvPourcent }` (rétrocompatible, tous les appels existants à 1-2 arguments inchangés). `statsPlates` fusionné comme le Parcho ; `RETRAIT_PA_TOTAL`/`RETRAIT_PM_TOTAL` lisent maintenant aussi `RETRAIT_PA_BRELOQUE`/`RETRAIT_PM_BRELOQUE` (clés dédiées, pas de stat cube équivalente — cf. section "Stats dérivées" plus haut, enfin comblé) ; `ESQUIVE_PA`/`ESQUIVE_PM` de breloque partagent la même clé que la stat cube, s'additionnent naturellement ; `VITALITE_TOTALE` applique `pdvPourcent` (Hémophile) en **multiplicateur** sur le total (base + cubes), pas en addition — seule stat de ce module désormais arrondie dans `calculerStatsPersonnage` (pas de calcul ultérieur qui en dépend). `calculerDegats(statsPersonnage, sortsEquipes, { multiplicateursBreloques, modeAttaque })` — 3ᵉ paramètre optionnel (rétrocompatible), le multiplicateur combiné s'applique en facteur final sur les dégâts normaux ET critiques.
- **`personnagesController.js`** (`construireFichePersonnage`) : calcule `effetsBreloques` à partir des breloques équipées (réutilise `construireBoost` déjà appelé pour l'onglet Boosts, pas de logique dupliquée), le passe à `calculerStatsPersonnage`. `degats` n'est plus un tableau mais **`{ distance: [...], melee: [...] }`** — les deux modes sont calculés d'avance côté serveur à chaque `GET`, pour que basculer le toggle Distance/Mêlée côté client soit instantané (pas de round-trip réseau).
- **Frontend** : `OngletSorts.jsx` récupère 2 réglages locaux non persistés (juste une préférence d'affichage, pas de sens à sauvegarder) :
  - **Distance/Mêlée** (défaut Distance) : sélectionne `personnage.degats.distance` ou `.melee`.
  - **Dégâts de base/calculés** (défaut calculés) : bascule entre `<SortCard sort={sort} calcul={...} />` et `<SortCard sort={sort} />` (prop `calcul` déjà optionnelle depuis la Tâche 8, aucun changement nécessaire dans `SortCard.jsx`).
  - Les 2 réglages affichés comme 2 groupes de boutons façon "segmented control" (même convention visuelle que les filtres de rang/onglets existants), pas un switch on/off — plus lisible avec des libellés explicites que "toggle" au sens strict.
- **Bug trouvé et corrigé pendant les tests manuels** : après avoir remplacé la mise à jour locale optimiste de `OngletBoosts` par un refetch (`onSauvegarde` = `rafraichir`), le premier essai montrait encore les anciens dégâts après avoir activé un boost à cause de la fenêtre de test rejouée sans recharger — une fois `rafraichir` bien câblé (voir "Onglet Boosts breloques" ci-dessus, déjà à jour), le comportement est correct : dégâts recalculés immédiatement après toute interaction dans l'onglet Boosts.
- Testé de bout en bout via un personnage jetable, en appelant directement les fonctions du contrôleur (contourne un backend de session parallèle non redémarré) : multiplicateur simple, addition entre même type (Sursaut × Sentinelle en mode distance = ×1.2×1.3 combinés en multiplication car types différents, vérifié = ×1.56), un multiplicateur "distance" ignoré en mode mêlée, bonus PA plat (Entrée fracassante), Retrait PA + Esquive PA (Embourbeur), PdV% (Hémophile) — tous les résultats correspondent aux calculs manuels attendus. Vérifié aussi en navigateur (mobile 375px, aucun débordement introduit par la nouvelle rangée de toggles) : bascule Distance/Mêlée instantanée, bascule Dégâts de base/calculés correcte, page de partage publique cohérente avec la fiche privée.
- **Ce qui reste simplifié/non géré** (aucune donnée ni demande actuelle ne le couvre) : "+1 bombes" mentionné dans une version antérieure de la description Bombeur fasciné n'apparaît plus dans les colonnes de bonus réellement importées (vérifié en base) — rien à faire. `DO_CRIT` (Forcené) et `%_COUP_CRITIQUE` (Forcené, Stratège d'avant-garde, Tension croissante) alimentent déjà `calculerStatsPersonnage` via ce chantier, mais `DO_CRIT` reste non appliqué au résultat final de `calculerDegats` (limite déjà connue, pas de modélisation du jet critique — voir plus bas).

### ✅ Retouche — texte écrasé sur les breloques à slider (mobile)
- Signalé par le porteur de projet avec capture d'écran (breloque "Rage mortelle") : sur mobile, le slider + son libellé (ex: "dommages finaux x1.75") prennent plus de la moitié de la largeur de la case, écrasant la colonne nom/effet jusqu'à 1 mot par ligne (`.onglet-boosts__info` a `min-width: 0`, qui autorise ce rétrécissement extrême en flexbox).
- Corrigé en ciblant **uniquement `.boost-range`** (le slider), sous une media query `max-width: 599px` (même palier que le reste du site) : slider et libellé empilés en colonne au lieu de côte à côte, slider réduit à 90px, libellé plafonné à 100px de large (il peut désormais passer sur 2 lignes, ex: "dommages finaux" / "x1.75"). N'affecte ni `.boost-toggle` ni `.boost-accumulateur` (classes distinctes, non touchées), ni le desktop (règle sous media query, vérifié : à 600px pile le slider redevient 110px en ligne, identique à avant ce correctif).
- Vérifié sur les 3 types de breloques équipées à la fois (range/toggle/accumulateur) : largeur du contrôle mesurée en JS avant/après, seule celle de la breloque range a changé.

### ✅ Images réelles des cubes (419) + nettoyage visuel des cases équipées
- `server/scripts/convert-cubes-webp.js` (Sharp, PNG→WebP) : nécessite Node 20+ (incompatible Node 18 local) — downgrade temporaire de `sharp` vers 0.32.6 pour lancer la conversion, restauré ensuite à la version normale du projet. Même procédure réutilisée plus tard pour les images des sorts "Unique" (voir "54 sorts manquants" ci-dessous).
- `server/scripts/import-cubes-images.js` : les fichiers sources n'ont pas de nom explicite (identifiants numériques bruts) mais leur **ordre** (tri numérique croissant) correspond à l'ordre alphabétique élément → rang → numéro croissant, confirmé par le porteur de projet — matching positionnel plutôt que par nom. Un seul cube sans image fournie (le dernier de l'ordre : Terre Rare n°15).
- **Nettoyage du style provisoire des cases cube équipées**, devenu inutile avec les vraies images : fond de couleur par élément retiré, puis bordure colorée par rang de cube retirée (nouvelle prop `sansBordure` sur `EmplacementSlot.jsx`, appliquée uniquement aux cases cube — les breloques/sorts gardent leur bordure de rang, cf. Tâche 7 Bout 4), puis le halo lumineux (`box-shadow`) des cubes Éxalté supprimé entièrement — `couleurRangCube` (`client/src/constants/rangs.js`) perd sa fonction `lueurRangCube`, qui n'existe plus.

### ✅ Gestion du personnage : renommer / tout déséquiper / supprimer
- 3 nouvelles routes dans `personnagesController.js`/`routes/personnages.js` : `PUT /:id/nom`, `PUT /:id/desequiper-tout`, `DELETE /:id`.
- **Confirmation "Tout déséquiper" via `Modal.jsx`** (composant déjà existant) plutôt que `window.confirm()` — testé en premier avec `window.confirm`, qui bloque toute automatisation navigateur (impossible à cliquer par les outils de preview) en plus d'être visuellement hors charte.
- UI : bouton renommer en petit carré discret à bordure fine (28×28), bouton supprimer en petit carré icône poubelle rouge (36×36, texte "supprimer ce personnage" au survol) — décisions explicitement demandées par le porteur de projet en réaction à un premier jet jugé trop imposant/rond.

### ✅ Dernière modification affichée + tri de la liste + compteur de vues du lien partagé
- `listerPersonnages` trié par `mis_a_jour_le DESC` (JOIN `Equipement`) au lieu de la date de création — le personnage tout juste modifié remonte en haut de "Mes personnages".
- Nouvelle fonction `marquerModifie(personnageId)`, appelée après chaque mutation réelle (équiper/déséquiper, Parcho, boost de breloque, renommage) pour maintenir `mis_a_jour_le` à jour.
- `client/src/utils/temps.js` (`tempsRelatif`) : affichage relatif français à **une seule unité** (jamais "il y a 1 mois et 3 jours", juste "il y a 1 mois"), affiché avec une icône horloge sur `PersonnagePage.jsx`.
- `vues_partage` (colonne `Equipement`) incrémenté à chaque consultation du lien public — avec `mis_a_jour_le = mis_a_jour_le` explicite dans la requête `UPDATE` pour **empêcher** le `ON UPDATE CURRENT_TIMESTAMP` automatique de la colonne de se déclencher : sans ça, un simple visiteur consultant le lien partagé aurait fait remonter le personnage dans la liste de son propriétaire, alors que seules les vraies modifications doivent le faire.
- **Incident prod** : la migration de la colonne `vues_partage` avait été appliquée en local mais pas sur le VPS avant le déploiement du code qui la référence → crash 500 (`ER_BAD_FIELD_ERROR`) sur `listerPersonnages` en prod, corrigé en appliquant la migration manquante après coup via SSH. Retenu comme rappel : toujours vérifier qu'une migration de schéma a bien été jouée en prod avant de déployer le code qui en dépend.

### ✅ Lien "Retour à ma fiche" sur les pages liste
- Nouveau composant `LienRetourFiche.jsx`, affiché en haut de `CubeListPage`/`SortListPage`/`BreloqueListPage` quand la query string contient `?perso=<id>` (navigation depuis la fiche perso pour équiper un item) — évite de devoir attendre le Toast de 3 secondes ou cliquer "Précédent" dans le navigateur pour revenir à sa fiche.
- Style : bordure/fond dorés discrets (même recette visuelle que réutilisée ensuite pour le bouton "Copier le lien de partage"), pas un gros bouton arrondi — demandé explicitement par le porteur de projet, qui n'aime pas ce style.

### ✅ Accessibilité (contraste WCAG) + réordonnancement des filtres de stats cubes
- Contrastes corrigés (vérifiés à l'outil WAVE) sur les inputs de recherche, le bouton de filtre "Tous", et la zone "+ de filtres" — `CubeListPage.css`/`SortListPage.css`/`BreloqueListPage.css`, couleurs choisies pour rester dans la charte du site plutôt qu'un simple gris/blanc générique.
- `client/src/constants/statsCubes.js` réordonné pour suivre le regroupement visuel de DofusBook (PA/PM/PO → caractéristiques → mobilité → dommages → résistances) au lieu de l'ordre alphabétique — toujours les mêmes 35 stats, aucun ajout/retrait.

### ✅ Petites corrections — plafond Parcho + bug du slider de boost breloque
- Plafond `PARCHO_MAX = 1000` ajouté côté serveur (`sauvegarderParcho`) et client (`StatsPersonnage.jsx`) — une valeur de Parcho sans limite pouvait causer des soucis serveur.
- **Bug du slider `RangeBoost`** (`OngletBoosts.jsx`) qui se bloquait après chaque cran glissé (il fallait relâcher puis recliquer à chaque fois) : la prop `disabled` (posée pendant la sauvegarde réseau) coupait le suivi natif du glisser-déposer du navigateur en plein milieu du geste. Retirée uniquement pour `RangeBoost` (un slider est un geste continu, contrairement à `ToggleBoost`/`AccumulateurBoost` qui restent désactivés pendant leur sauvegarde — ce sont des clics ponctuels, pas un geste continu).

### ✅ 54 sorts manquants ajoutés (comparaison avec touchmanager.fr)
- Le porteur de projet a fourni la liste des sorts affichés par touchmanager.fr (site tiers non-officiel) ; comparaison automatique avec les 115 sorts déjà en base → **52 sorts manquants identifiés** (40 à 4 rangs + 12 "Unique", probablement des sorts de boss).
- Tableau Excel généré (xlsx, feuille "Instructions" + feuille "Sorts à ajouter") pour que le porteur de projet saisisse les statistiques manquantes — colonnes-clés surlignées, listes déroulantes sur Rang/Portée modifiable/Ligne de vue requise, lignes pré-remplies en vert (Nom/Élément/Rang) pour les 52 sorts repérés. Renvoyé complété en CSV (`data/Dedalofus_Sorts_manquants_COMPLET.csv`), avec 2 sorts "Unique" ajoutés en plus par le porteur de projet (54 au total).
- **`server/scripts/import-sorts-manquants.js`** (rejouable, garde-fou anti-doublon par `nom`) :
  - Certains sorts existent en 2 variantes d'élément physique (ex: "Acharnement méthodique (Air)"/"(Feu)") — le porteur de projet n'a rempli qu'une des deux variantes ("toutes les infos sont pareilles, seul l'élément de frappe diffère"), le script copie les stats de la variante remplie vers sa jumelle vide.
  - Élément des sorts Chaos/Lumière (non renseigné dans le fichier touchmanager) déduit automatiquement : "Meilleur élément" si le sort inflige des dégâts sur au moins un rang (même convention que les sorts Chaos déjà en base), `NULL` sinon (sorts utilitaires/invocations).
  - Noms normalisés vers la convention déjà en base : `"Acharnement méthodique (Air)"` → `"Acharnement méthodique - Air"`, `"Appétit (Chaos)"` → `"Appétit"` (suffixe de catégorie retiré entièrement pour Chaos/Lumière).
  - Images associées par correspondance de mots (sorts réguliers, fichiers `sort_*_novice.webp` déjà présents en source) ou correspondance directe (les 14 sorts "Unique", noms de fichiers trop abrégés pour un matching fiable) — `server/scripts/convert-sorts-unique-webp.js` pour convertir leurs images PNG sources en webp au préalable.
  - **174 lignes insérées** (40×4 + 14 Unique), toutes avec une image associée. Exécuté et vérifié en local puis en prod (les ids générés côté prod se sont avérés identiques à ceux du local, bases parfaitement synchronisées).
- En vérifiant, décalage repéré entre le nombre de sorts ayant un `image_url` en base (448) et le nombre de fichiers physiquement présents dans `client/public/images/sorts/` (244) — **antérieur à cet import** (les 174 nouvelles lignes ont toutes leur fichier, vérifié), signalé mais pas corrigé (hors périmètre de la demande).

### ✅ Fix carte monstre géante sur desktop (bug "Lazuli")
- Signalé par le porteur de projet : sur la page `/monstres` en largeur desktop (≥900px), une carte monstre à l'image très haute/étroite (ex: Lazuli) forçait toute la carte à devenir démesurément haute, alors que d'autres monstres avec des images plus "normales" restaient compacts.
- Cause : `.carte-monstre__image` (`MonstreCard.css`) passait à `height: auto` sur ce point de rupture (image à gauche = 30% de largeur, carte à l'horizontale) — sans hauteur fixée, la case suivait le ratio naturel du fichier source appliqué à sa largeur, au lieu d'être plafonnée. Corrigé en repassant à une hauteur fixe (`170px`, même valeur que la tablette) : `object-fit: contain` (déjà en place) réduit alors l'image pour qu'elle tienne dans la case fixe, au lieu de forcer la case à s'agrandir.
- Notion générale utile pour la suite : sur le web, un fichier image ne contrôle jamais une taille en pixels absolue à l'écran — seulement un ratio (si une dimension du conteneur est libre) ou rien du tout (si les deux dimensions du conteneur sont fixées en CSS).

### ✅ Embed Discord (og-image) resynchronisé avec le style réel du site
- `server/routes/ogImage.js` (génération d'image satori/resvg pour l'aperçu Discord d'un stuff partagé) avait pris du retard sur les changements CSS du site :
  - Cubes : bordure + fond teintés par élément → supprimés (comme sur le site depuis les vraies images de cubes, voir plus haut).
  - Sorts/breloques : bordure colorée par **élément** → corrigée en bordure colorée par **rang de maîtrise** (reprend `couleurRangMaitrise`), comme sur la vraie fiche perso — la requête SQL de l'image ne remontait même pas encore la colonne `rang`/`rang_evolution` nécessaire.
  - `object-fit: cover` (rognait l'image) → `contain` (comme partout ailleurs sur le site).

### ✅ Sorts multi-lignes (Bluff, Tourbillon Embrasé, Pile ou Face, Foène, Pelle Aveuglante) + sorts de soin
- **Nouvelle table `SortDegatsSup`** (`sort_id`, `ordre`, `element` nullable = hérite de l'élément du sort, `degats_min/max`, `degats_critique_min/max`) : lignes de dégâts indépendantes de la ligne principale du `Sort` — élément différent avec des valeurs différentes (Tourbillon Embrasé : 4 lignes, 2 par élément), ou 2e ligne qui n'existe qu'au critique (Pile ou Face).
- **`Sort.est_soin`** (nouvelle colonne) + formule de soin dédiée dans `calcul.js` (`calculerStatEfficaceSoin`/`calculerBonusSoin`/`choisirMeilleurElementSoin`) : même structure `ax+b` que les dégâts, mais **sans Puissance** et avec la stat `Soin` à la place de Dommages — jamais affectée par les multiplicateurs de dégâts des breloques (mécaniquement incohérent de boostr un soin avec un bonus de dégâts).
- `calculerDegats` restructuré : au lieu d'une seule ligne par sort, construit la liste des lignes (principale + `SortDegatsSup`) et résout chacune indépendamment vers 1 ou 2 éléments de frappe — `degatsMin/Max` désormais optionnels par ligne (absents si la ligne n'existe qu'au critique), comme l'étaient déjà `degatsCritiqueMin/Max`. 10 nouveaux tests unitaires (112 au total).
- **Bug latent découvert au passage** : Bluff/Tourbillon Embrasé stockent leur élément double sous forme de chaîne texte (`"Air, Eau"`) en base, mais `personnagesController.js` la transmettait telle quelle à `calculerDegats`, qui n'accepte un double-élément que sous forme de **tableau** JS (`['Air', 'Eau']`) — ces sorts ne produisaient donc jamais de dégâts calculés en pratique jusqu'ici. Corrigé (`eclaterElement`, découpe sur la virgule).
- **Corrections de données** (`server/scripts/corrige-sorts-multi-lignes.js`, rejouable) : Escarre face (dégâts fixes 10 Novice / 12 Expert-Maître, jamais renseignés), Toxines (élément "Meilleur élément" manquant), valeurs de PV rendus de Mot Soignant/Curatif/Revitalisant (récupérées dans les CSV sources — la colonne "Effet du sort" contenait déjà ces valeurs, explicitement ignorées par `parseDegatsDepuisEffet` car filtrées comme "PV rendus" lors de l'import initial), 2èmes lignes de Pelle Aveuglante/Pile ou Face/Foène/Tourbillon Embrasé.
- `SortCard.jsx` affiche désormais **N lignes de dégâts/soin empilées sur une seule carte** (libellé "Soin (Élément)" ou "Dégâts (Élément)" selon `est_soin`) au lieu d'une carte séparée par élément — `OngletSorts.jsx` simplifié en conséquence (regroupe tous les résultats de `calculerDegats` d'un même sort dans une seule carte).

### ✅ Liens de partage raccourcis (32 → 8 caractères)
- `crypto.randomBytes(16).toString('hex')` (32 caractères hexadécimaux) → générateur base62 (chiffres + lettres maj/min, alphabet à 62 caractères) sur 8 caractères — ~218 000 milliards de combinaisons, largement suffisant pour ce volume d'utilisateurs, inspiré des liens courts façon DofusBook.
- Génération avec rejet des octets au-delà du dernier multiple de 62 (évite un léger biais de modulo), nouvelle tentative en cas de collision improbable (contrainte `UNIQUE` déjà en place).
- **Décision explicite du porteur de projet** : seuls les personnages créés à partir de maintenant ont un lien court — les personnages déjà existants gardent leur ancien lien à 32 caractères (toujours fonctionnel), pour ne pas casser les liens déjà partagés.

### 🔄 Cache d'aperçu Discord — tentatives de correction, limitation non contournable identifiée
- Signalé par le porteur de projet : modifier un stuff après l'avoir déjà partagé sur Discord n'actualise jamais l'aperçu du message déjà posté, même plusieurs jours après.
- **Tentative 1** : bouton "Copier le lien de partage" ajoute `?v=<horodatage>` à chaque copie, pour que Discord traite chaque partage comme une URL jamais vue. Vérifié fonctionnel côté serveur (curl avec l'User-Agent de Discord), mais le porteur de projet a testé en conditions réelles (nouveau message Discord, lien fraîchement copié) et **ça ne suffisait pas**.
- **Tentative 2** : remplacement du paramètre `?v=` par un **segment de chemin** (`/partage/<lien>/<horodatage>`), jugé plus robuste (un chemin ne peut pas être normalisé/ignoré comme une query string peut l'être par certains crawlers). Piège rencontré : Express 5 utilise `path-to-regexp` v8, qui a **retiré** l'ancienne syntaxe `:param?` d'Express 4 pour les paramètres optionnels — la bonne syntaxe est désormais `{/:param}` (groupe optionnel). Crash immédiat au démarrage du serveur avec l'ancienne syntaxe, repéré et corrigé avant tout déploiement prod. Note : react-router-dom (v7, côté client) garde lui la syntaxe `:param?` classique — les deux bibliothèques ont des règles différentes, à ne pas confondre.
- **Diagnostic final, après recherche** (y compris l'issue GitHub officielle non résolue `discord/discord-api-docs#1663`) : Discord met en cache les aperçus de lien selon son propre système interne, dont la durée n'est **pas documentée officiellement et n'est pas garantie instantanée même pour une URL techniquement neuve** — les sources indépendantes convergent vers une fourchette de 30 minutes à plusieurs heures. Il n'existe aucun mécanisme public pour forcer un rafraîchissement immédiat. Conclusion : **limitation de la plateforme Discord, non réparable côté site** — le mécanisme d'URL unique mis en place reste la bonne pratique (il garantit qu'un rafraîchissement finira par arriver), mais ne garantit pas l'instantanéité. Aucune piste de correction supplémentaire à explorer côté code.

### 🔄 Page "Ensembles" + branchement au calcul (images en attente)
- **Nouveau concept, distinct des panoplies de cubes** : le Dédale compte **29 ensembles** au total — les 5 familles de panoplie de cubes existantes (inchangées, cf. section "Stats dérivées et bonus de panoplie" ci-dessus) + **10 ensembles "classiques"** (mélange sorts/breloques, ex: Ensemble du Pugiliste) + **14 ensembles de boss** (toujours 1 breloque + 1 sort, dropés sur le boss correspondant). Décrits en intégralité dans un nouveau fichier `data/Liste-des-ensembles.md` fourni par le porteur de projet, accompagné de `data/sorts-degats-indirects.md` (liste des sorts concernés par les nouvelles stats "dommages indirects"/"pièges"). "Bonus panoplie" renommé "**Bonus d'ensemble**" à l'affichage (`PanopliesPersonnage.jsx`, texte seulement — aucun renommage interne de fichiers/variables).
- **Contrairement à l'onglet "Boosts breloques"** (affichage d'abord, calcul ensuite dans un chantier séparé), le porteur de projet a demandé le branchement réel au calcul dès ce chantier : équiper assez de pièces d'un ensemble classique/boss modifie réellement les stats/dégâts affichés sur la fiche perso, pas seulement un texte catalogue.
- **Pipeline de données** — nouveau script **`server/scripts/generate-ensembles.js`** (rejouable, aucune écriture en base) : parse `Liste-des-ensembles.md` (machine à états, même esprit que `generate-monstres.js`) puis résout chaque pièce nommée contre la vraie base (`Sort`/`Breloque`) via plusieurs passes successives — exact, casse, apostrophes typographiques (`'`/`’`) normalisées vers droites, préfixe réel des noms de breloques ("Breloque du Rixeur Instable" en base vs "Rixeur instable" dans le `.md`), suffixe d'élément implicite pour les sorts mono-élément non parenthésés en base ("Sismobombe" → "Sismobombe - Terre"), et une petite table d'alias (`ALIAS_NOMS`) pour les 5 derniers écarts confirmés un par un par requête directe (ex: "Sablier **de** Xélor" dans le `.md` vs "Sablier **du** Xélor" en base, "Funérailles anciennes" vs le vrai nom "Funérailles Aériennes"). **137/137 pièces classiques+boss résolues** après ces passes. Écrit le JSON généré à 2 emplacements identiques : `client/src/data/ensembles.json` (import direct, comme `monstres.json`) et `server/data/ensembles.json` (`require` par le contrôleur pour le calcul, évite un aller-retour réseau).
  - Les **ensembles de cubes** (5) ne viennent **pas** du `.md` (ses valeurs pour ce groupe sont confirmées fausses par le porteur de projet, cf. section panoplies ci-dessus) — reconstruits directement depuis `PANOPLIES` (exporté par `calcul.js` pour l'occasion), seule source de vérité.
  - ⚠️ **1 doublon signalé par le rapport de génération, pas encore résolu** : la breloque "Protection du Craqdoa" est utilisée à la fois par "Ensemble du Phossile" et "Ensemble du Craqdoa" dans le `.md` — très probablement une coquille de copier-coller pour Phossile (dont la description parle d'un glyphe qui retire des PM, sans rapport avec "Protection du Craqdoa" qui pose un bouclier). Une breloque `"Proximité étouffante du Phossile"` existe en base et n'est utilisée par aucun ensemble — candidate évidente, mais **pas appliquée automatiquement** (pas de correction devinée sans confirmation, contrairement aux 5 alias ci-dessus qui étaient non-ambigus). À trancher par le porteur de projet puis ajouter à `ALIAS_NOMS` ou corriger le `.md`, puis relancer le script.
  - Note capturée mais **pas exploitée pour le comptage** : 3 sorts (Griffe joueuse, Schnaps, Épée Céleste, ensembles Bousculeur/Calculateur) ont une annotation "(Maître β uniquement, ...)" dans le `.md` — sens exact ambigu (restriction de rang pour le comptage de l'ensemble, ou simple remarque). Stockée telle quelle dans `piece.note` du JSON généré, mais `calculerEnsemblesActifs` compte actuellement ces pièces à n'importe quel rang équipé comme toutes les autres (cohérent avec la règle générale "une pièce compte peu importe son rang", déjà utilisée pour la contrainte anti-doublon des sorts/breloques).
- **Sorts à dégâts indirects et pièges** (`sorts-degats-indirects.md`) — mécanique donnée explicitement par le porteur de projet ("Do et Puissance pièges fonctionnent pareil que dommages et puissance mais uniquement pour les pièges"), branchée réellement (pas juste affichée) :
  - 2 nouvelles colonnes `Sort.est_indirect`/`Sort.est_piege` (`schema.sql` + migration `server/scripts/migrate-sorts-indirects-pieges.js`, rejouable) — 16 sorts indirects marqués (dont 5 pièges, sous-ensemble), résolus par nom avec la même normalisation apostrophes/préfixe que le générateur d'ensembles.
  - `calcul.js` : `calculerStatEfficace`/`calculerBonusDommages`/`calculerDegatsPourElement` acceptent un flag `estPiege` optionnel (défaut `false`) qui ajoute `PUISSANCE_PIEGE`/`DO_PIEGE` au calcul — aucune formule inventée, extension directe de la formule `ax+b` existante. `calculerDegats` sépare désormais les multiplicateurs en 2 groupes : "généraux" (`finaux`/`finaux_distance`/`finaux_melee`, inchangés, s'appliquent à tous les sorts) et **`'indirects'`** (nouveau, ne s'applique qu'aux sorts `estIndirect` — bonus d'ensemble "% de dommages indirects", ex: Ensemble de l'Artificier/Toxoliath), les deux facteurs se multipliant entre eux pour un sort à la fois indirect et concerné par un multiplicateur général.
  - `TACLE_TOTAL` a maintenant un terme brut additif (`+ (stats.TACLE || 0)`), comme `FUITE_TOTALE` avait déjà — nécessaire pour que "+N Tacle" (Ensemble du Pugiliste) ait un effet, aucune stat cube équivalente n'existant jusqu'ici.
  - "**% résistances distance/mêlée**" (concept différent, mentionné séparément dans `sorts-degats-indirects.md`) : confirmé volontairement **indicatif seulement** par le porteur de projet ("rien ne changera dans l'affichage des stats") — texte affiché sur la card d'ensemble, aucune clé de calcul associée. Ne pas confondre avec "% dommages indirects" ci-dessus, qui lui est bien calculé.
- **Nouveau module `server/logic/ensembles.js`** (+ `ensembles.test.js`, 10 tests) : `calculerEnsemblesActifs(sortsEquipes, breloquesEquipes, ensemblesData)` compte, par ensemble classique/boss, combien de ses pièces nommées sont équipées (comparaison par `nom`, rang ignoré — même principe que `trouverEmplacementDoublonParNom`), détermine le palier atteint et si le "bonus spécial" est débloqué ; `calculerBonusEnsembles` fusionne les deltas de tous les ensembles actifs en `{ statsPlates, multiplicateurs }`, même forme que `calculerEffetsBreloques` pour se brancher aux mêmes points d'entrée. Les deltas par palier (texte → `{statsPlates, multiplicateurs}`) sont pré-parsés une fois pour toutes au moment de la génération du JSON (table `LIBELLE_VERS_CLE` dans `generate-ensembles.js`, même esprit que `resoudreTexte`/`boosts.js`), pas à chaque calcul.
- **`personnagesController.js`** (`construireFichePersonnage`) : `require` de `server/data/ensembles.json` une fois au chargement du module. Calcule `ensemblesActifs` à partir des sorts/breloques équipés, passe `calculerBonusEnsembles(...)` en 4ᵉ argument à `calculerStatsPersonnage` (fusionné exactement comme Parcho/breloques) et concatène ses multiplicateurs à ceux des breloques avant les 2 appels `calculerDegats` (distance/mêlée). Expose `ensemblesActifs` dans la réponse (`{ ..., ensemblesActifs }`), même niveau que `panoplies` (qui reste dédié aux ensembles de cubes, mécanisme inchangé).
- **Vérifié en conditions réelles** via un personnage jetable créé/équipé/supprimé par script (compte + personnage nettoyés après coup, rien laissé en base) : 3 pièces de l'Ensemble du Fourbe (palier 3, +50 Puissance Pièges +5 Do Pièges) simultanément actives avec l'Ensemble de Toxoliath (1 pièce, palier 1, +100 Puissance +10% dommages indirects, déclenché par un sort partagé entre les 2 ensembles) — dégâts d'un sort piège recalculés à la main (stat efficace = 0+100+50, bonus = 0+0+5, ×1.1 indirects) correspondent exactement à la réponse API. Confirme le cumul correct de plusieurs ensembles actifs simultanément, y compris quand une même pièce équipée compte pour 2 ensembles différents à la fois.
- **Frontend** :
  - Nouvelle page **`/ensembles`** (`EnsembleListPage.jsx`), liée depuis le header ("Liste des ensembles", entre "Liste des items" et "Liste des monstres", poids visuel neutre comme "Liste des monstres" — pas le traitement doré de "Mes stuffs") : 3 sections (cubes/classiques/boss), grille de `EnsembleCard` en 3 colonnes fixes desktop / 1 mobile (même convention que `MonstreCard`/`MonstreListPage.css`, vérifié sans débordement horizontal aux 2 largeurs). Chaque card = aperçu tronqué (pièces + bonus, "…") cliquable → `Modal large` + `EnsembleDetail` (encart "Bonus spécial" séparé, tous les paliers, pièces éclatées par rang comme sur touchmanager.fr — référence visuelle fournie par le porteur de projet).
  - **`EnsembleLien.jsx`** (nouveau, autonome — possède sa propre modale, évite de faire remonter un état dans chaque page qui rend une card) : affiche un lien "Ensemble de xxx" sur `CubeCard`/`BreloqueCard`/`SortCard` quand l'item appartient à un ensemble (`client/src/utils/ensembles.js`, lookup par élément pour les cubes — un cube Chaos matche les 5 familles à la fois — ou par nom pour sorts/breloques). Vérifié : le clic n'entraîne pas la navigation/l'action de la card parente (`stopPropagation`/`preventDefault`).
  - **`EnsemblesPersonnage.jsx`** : liste des ensembles classiques/boss actifs (`personnage.ensemblesActifs`) sur la fiche perso et la page de partage, à côté de `PanopliesPersonnage` — chaque ligne cliquable vers le détail complet (relit `ensembles.json` par `cle`, le résumé serveur n'a pas la liste complète des pièces/paliers).
  - `StatsPersonnage.jsx` : 2 nouvelles lignes dans le bloc Dommages ("Do Piège", "Puiss. Piège" — stats réellement fonctionnelles désormais, pas juste décoratives). Pas de ligne dédiée pour "% dommages indirects" (multiplicateur, pas une stat brute accumulée) : même convention que les multiplicateurs "dommages finaux" des breloques, invisibles en dehors des dégâts calculés sur `OngletSorts`.
- **Vignettes composites des ensembles (2 pièces en diagonale)** : le porteur de projet a fourni les 2 pièces représentatives des 15 premiers ensembles (5 cubes + 10 classiques — toujours "Cube [Élément] Commun n°1 + Éxalté n°1" pour les cubes) et confirmé le principe visuel via un mockup (2 icônes qui se chevauchent, celle de gauche/bas au premier plan).
  - **`IMAGE_ITEMS_CLASSIQUES`** (`generate-ensembles.js`) : table des 2 pièces par ensemble classique, résolues via le même `resoudrePiece` que les listes de pièces. **`chargerCubesImageItems()`** : requête dédiée (Commun/Éxalté n°1 par famille) pour les 5 ensembles de cubes. Résultat stocké dans `imageItems: [{type,id,imageUrl}, {type,id,imageUrl}] | null` par ensemble (`null` si l'une des 2 images manque — cf. boss ci-dessous).
  - **Nouveau script `server/scripts/generate-ensembles-images.js`** (nécessite `sharp`, donc **Node >= 20** — plante sous le Node 18 utilisé en local pour le reste du projet, même contrainte que `convert-cubes-webp.js`) : détoure les breloques/sorts par flood-fill depuis les 4 bords (fond noir opaque, pas de canal alpha — contrairement aux cubes, déjà transparents, non détourés), redimensionne, puis compose chaque paire en diagonale (320×320, item 220×220, celui du 1er `imageItems` en bas-gauche/premier plan par-dessus celui du 2e en haut-droite/arrière-plan) → `client/public/images/ensembles/<cle>.webp`, et ajoute `imageComposite` aux 2 copies de `ensembles.json`. Qualité vérifiée visuellement (y compris composité sur fond sombre `#1F1C18`, la vraie couleur de fond du site) : aucun liseré noir résiduel.
  - **15/29 ensembles ont leur vignette** (5 cubes + 10 classiques). **Les 14 ensembles de boss n'en ont pas** : leurs 2 pièces sont bien celles de l'ensemble lui-même (imposé, pas un choix à faire), mais les 14 breloques de rang Unique (une par boss) n'ont **aucune image en base pour l'instant** — `generate-ensembles-images.js` détecte l'absence et journalise `"<cle> : image manquante..."` sans planter. À relancer tel quel dès que ces images arriveront.
  - **Effet de bord découvert et corrigé en marge** : le `sharp@0.35.3` déjà déclaré dans `server/package.json` ne se chargeait plus du tout (`Could not load the sharp module... win32-x64`, y compris sous Node 20+) — le binaire natif Windows manquait dans `node_modules`. Corrigé par `npm install --include=optional sharp` (une fois sous Node 20+) : ajoute l'entrée du binaire optionnel dans `package-lock.json` uniquement (`package.json` inchangé), laissé en place volontairement (fix légitime, pas un hack à annuler) — **corrige probablement au passage** la génération d'aperçu Discord (`og-image.js`), désactivée pour la même raison exacte (`[og-image] Dépendance manquante`), jamais testé explicitement.
  - ⚠️ **Point d'attention laissé en suspens** : passer sous Node 20+ pour lancer ce script nécessite de changer la version Node active (`nvm use`). `nvm use 18.20.2` pour revenir à la version normale du projet a échoué en fin de session (la commande reste bloquée, probablement une invite d'élévation Windows (UAC) qui ne peut pas être validée par un outil automatisé) — **la version Node active sur cette machine est potentiellement restée sur 24.x au lieu de 18.20.2 habituel**. À vérifier/corriger en premier à la prochaine session (`node --version`, puis `nvm use 18.20.2` si besoin — depuis un terminal utilisateur normal, l'invite UAC pourra être validée normalement).
  - Le doublon "Protection du Craqdoa" (Phossile/Craqdoa) et la question "Maître β uniquement" ci-dessus, à trancher par le porteur de projet.

### ✅ Retours du porteur de projet sur la page Ensembles — corrigés
- **Bug modale transparente + couleurs incohérentes selon l'entrée** : `Modal.jsx` ne pose lui-même ni fond ni couleur sur `.modale__contenu` — c'est toujours le composant de contenu qui le fait (`.carte-cube`, `.detail-monstre`... ont déjà `background: var(--panneau-fond); color: var(--panneau-texte);`). `EnsembleDetail.css` ne le faisait pas : la modale apparaissait donc transparente, et son texte héritait de la couleur du **vrai parent DOM** (`Modal.jsx` ne rend pas dans un portail) — le fond sombre du site quand ouverte depuis `/ensembles` (texte clair, lisible "par hasard"), mais le texte sombre d'une card `.carte-cube`/`.carte-breloque`/`.carte-sort` (fond clair) quand ouverte depuis un lien d'item, d'où le texte quasi invisible signalé uniquement dans ce 2ᵉ cas. Corrigé en ajoutant le même triplet fond/couleur/bordure que les autres contenus de modale directement sur `.detail-ensemble`.
- **Croix de fermeture de `Modal.jsx` redessinée** (composant unique, partagé par toutes les modales du site — un seul fix suffit) : SVG (2 traits) plutôt qu'un glyphe "×" (rendu inconsistant selon police/OS), fond `--bg-surface` + bordure `--bordure-or-forte` + ombre portée (plus de "flotte sans profondeur"), rotation 90° + accent violet au survol.
- **Bonus d'ensemble affichés 1 par ligne avec icônes**, plus jamais regroupés (ex: "Retrait PA/PM" → 2 lignes "Ret. PA"/"Ret. PM" distinctes) : nouveau fichier `client/src/constants/iconesEnsembles.js`, libellés/icônes/couleurs **recopiés à l'identique de `StatsPersonnage.jsx`** (demande explicite : toujours "Vitalité", jamais "Points de vie"/"PV") — à retenir pour tout futur ajout de stat, garder les 2 fichiers synchronisés à la main (pas de source commune factorisée, jugé pas nécessaire pour ce périmètre). `generate-ensembles.js` (`parserBonusTexte`) expose désormais aussi `notes` (texte brut conservé pour tout segment sans delta calculable : "% résistances distance/mêlée" resté volontairement indicatif, "Aucun bonus", tout segment non reconnu) — rien n'est plus silencieusement perdu à l'affichage.
  - **1 seule colonne retenue** pour la liste de stats à l'intérieur d'une card palier (pas de 2 colonnes internes) : mesuré en conditions réelles sur le pire cas (Ensemble des Portes de la Mort, palier "résistances fixes" = 7 lignes) — la card palier ne fait que ~291px de large même en plein desktop (la modale elle-même est plafonnée à 640px puis divisée en 2 colonnes de cards), pas assez pour 2 colonnes de stats sans les compresser ; la hauteur obtenue (210px pour 7 lignes) reste raisonnable dans le scroll de la modale (`max-height: 85vh`). La grille de cards palier, elle, garde ses 2 colonnes à partir de 600px (inchangé).
- **Bonus d'ensemble pas dynamiques au déséquipement** : `PersonnageDetailPage.jsx` (`desequiper`) faisait un patch local de l'emplacement seul (`setPersonnage` sur juste `cubes`/`sorts`/`breloques`), jamais un refetch — donc `stats`/`panoplies`/`ensemblesActifs`/`degats` (recalculés uniquement côté serveur) restaient périmés jusqu'au prochain chargement de page. Corrigé en appelant `rafraichir()` (refetch complet) après le déséquipement, même principe déjà utilisé par `onParchoSauvegarde`/`onSauvegarde` (Boosts breloques) — `CLE_LISTE_PAR_TYPE`, devenu inutile, supprimé. Vérifié en conditions réelles (personnage jetable) : déséquiper 1 cube sur 2 d'une panoplie active fait immédiatement disparaître le bonus (+50 Vitalité) et le bloc `PanopliesPersonnage` entier, sans réactualiser la page.
- **Images des 14 breloques de boss fournies par le porteur de projet** (`data/images/breloques/*.png`, noms abrégés type `phor-proximite.png`) : nouveau script **`server/scripts/import-breloques-boss-images.js`** (correspondance en dur, vérifiée un par un contre les 14 lignes `rang = 'Unique'` — pas de matching flou nécessaire vu le petit nombre) convertit en webp et met à jour `Breloque.image_url`. Au passage, `phor-proximite.png` confirme définitivement que la breloque de l'Ensemble du Phossile est bien **"Proximité étouffante du Phossile"** (et non "Protection du Craqdoa") — corrigé dans `generate-ensembles.js` (`CORRECTIONS_BOSS_BRELOQUE`), le doublon signalé plus haut est résolu.
- **Les 29/29 ensembles ont maintenant leur vignette composite** (les 14 de boss étaient bloqués uniquement par l'absence d'image breloque, résolu ci-dessus) — `generate-ensembles-images.js` relancé, plus aucun placeholder sur `/ensembles`.
- Détail technique de session : pour les scripts nécessitant `sharp` (Node >= 20) sans vouloir changer la version Node active du poste (le `nvm use` avait posé problème lors du chantier précédent), invoquer directement le binaire d'une version spécifique déjà installée par nvm4w plutôt que de re-basculer le symlink : `"/c/Users/natha/AppData/Local/nvm/v24.11.0/node.exe" server/scripts/xxx.js` — évite complètement le problème, à réutiliser la prochaine fois plutôt que `nvm use`.

### ✅ Design de la page Ensembles refait — le correctif précédent avait un vrai bug de contraste
- **Cause racine du "texte noir sur fond noir"** : `.detail-ensemble__palier`/`.detail-ensemble__piece` gardaient un fond `--bg-surface` (thème sombre) hérité d'un premier jet, alors que `.detail-ensemble` (corrigé juste avant) était passé à `--panneau-fond` (clair) — texte `--panneau-texte` (brun foncé) sur fond quasi noir ≈ **1.46:1** de contraste réel (calculé), très en dessous du minimum WCAG AA (4.5:1). Même chose pour `--or`/`--accent` (sous-titre, lien d'ensemble au survol sur les cards Cube/Breloque/Sort) utilisés directement sur `--panneau-fond` : ces teintes n'avaient jusqu'ici jamais été testées que sur le thème sombre, ≈ **1.2 à 1.5:1** sur fond clair. Le composant mélangeait deux familles de tokens (clair "panneau" / sombre "bg-surface, accent, or, texte-attenue") sans jamais vérifier le rendu réel une fois combinées — **erreur de vérification, pas juste de choix de couleur** : le contrôle précédent ne portait que sur le fond racine, jamais sur les éléments enfants.
- **Corrigé en restant dans une seule famille de tokens du début à la fin** : `--panneau-fond-sombre` (pas `--bg-surface`) pour les cards internes, `--panneau-texte`/`--panneau-texte-attenue` pour tout le texte, `--primaire` (pas `--or`/`--accent`, jamais assez sombre sur ce fond) pour les accents de couleur — contrastes recalculés à la main pour chaque paire (`--panneau-texte` sur `--panneau-fond-sombre` ≈ 8:1, `--primaire` sur les 2 fonds clairs ≈ 5,7 à 6,5:1). `EnsembleLien.css` (lien "Ensemble de xxx" sur les cards) : le survol passait aussi par `--or`, illisible — remplacé par une inversion fond plein `--primaire` + texte `--primaire-texte` au survol (contraste garanti par construction, même logique que les autres boutons du site).
- **Nouveau composant partagé `BonusPalier.jsx`/`.css`** (extrait de `EnsembleDetail.jsx`) : icône **à gauche** de chaque ligne (pas à droite), plus de signe "+" (`[icône] 40 Agilité`, pas `+40`). Utilisé à la fois par `EnsembleDetail.jsx` (modale, contexte clair "panneau") et **`EnsemblesPersonnage.jsx`** (fiche perso, contexte sombre — n'utilisait jusqu'ici que `ensemble.bonusTexte` en texte brut non éclaté, jamais mis à jour lors du premier passage). Le composant ne fixe **aucune couleur de texte en dur** : `color: inherit` + `opacity: 0.7` pour l'atténué — s'adapte automatiquement aux 2 contextes puisque chaque parent définit déjà la bonne couleur pour son propre fond, sans dupliquer de CSS par thème.
- **Croix de fermeture réellement repensée** (`Modal.jsx`/`.css`, composant unique et partagé — un seul fix suffit partout) : le 1er essai n'était qu'un remaniement (même cercle flottant en dehors du coin, juste une rotation ajoutée) à raison signalé comme "rien n'a changé". Refait entièrement : pastille "verre dépoli" en incrustation (pas flottante hors du cadre), coins arrondis 8px (pas un cercle — aucun autre bouton du site n'est circulaire), couleur de fond fixe (indépendante du contenu clair ou sombre en dessous, donc toujours lisible sans calcul de contraste par contexte).
- **Scrollbar de la modale stylée** (`.modale__corps`) : fine, dorée, coins arrondis (Firefox `scrollbar-width`/`-color` + `::-webkit-scrollbar-*`) au lieu de la barre système par défaut.
- **Bug "clic sur la croix/le fond ferme la modale ET navigue vers /cubes/:id"** (uniquement depuis la liste des Cubes, jamais Breloques/Sorts) : `CubeListPage` est la seule des 3 pages liste à envelopper ses cards dans un `<Link>` (pas de page de détail dédiée pour Breloques/Sorts) — d'où le caractère "cube seulement". `Modal.jsx` ne rend pas dans un portail (`EnsembleLien` est un vrai descendant DOM du `<Link>`), et seul `.modale__contenu` appelait `stopPropagation()` ; ni le fond ni la croix ne le faisaient. **1er correctif insuffisant** : ajouter `stopPropagation()` seul empêchait le `onClick` de `react-router`'s `<Link>` (qui appelle lui-même `preventDefault()` avant de naviguer en client-side) de s'exécuter — sans lui, plus rien n'empêchait le comportement par défaut **natif** du `<a>` (navigation complète), le bug persistait sous une autre forme. Il fallait `preventDefault()` explicite en plus de `stopPropagation()`, ajouté sur les 3 points de fermeture (fond, croix, `EnsembleLien` lui-même). Vérifié en conditions réelles (clic backdrop et clic croix) : reste bien sur `/cubes`.
- **Bug de marge "les pièces se cognent à droite" (signalé uniquement en ouvrant depuis une card Breloque/Sort, pas depuis `/ensembles`)** : non reproduit après la refonte ci-dessus (mesuré : marges gauche/droite identiques et symétriques dans les 2 cas, desktop et mobile). Vraisemblablement lié à l'ancien fond `--bg-surface` des cards internes avant ce correctif. À re-signaler avec une capture si le bug persiste après ce déploiement — pas confirmé résolu par construction, seulement non reproductible en l'état actuel du code.
- **Portée de l'audit design** : les fichiers CSS déjà lus pendant ce chantier (CubeCard/BreloqueCard/SortCard/MonstreCard/MonstreDetail/StatsPersonnage/PanopliesPersonnage/Header/CubeListPage/index.css) associent tous correctement `--panneau-fond`+`--panneau-texte` (jamais mélangés avec les tokens du thème sombre) — l'erreur de contraste semble contenue au nouveau code Ensembles, pas un problème préexistant ailleurs sur le site. Ceci n'est **pas** un audit exhaustif (pages Sorts/Personnage/Connexion/Inscription/Footer/mentions légales non relues pendant ce chantier) — à faire si le porteur de projet le demande explicitement.

### ✅ Bonus d'ensemble restants encore affichés à l'ancienne (texte brut, sans icône, avec "+") — corrigé
- Signalé par le porteur de projet avec 4 captures : "10%/20% de résistances mêlées" (Ensemble de Guerre), "5%/10% de résistances distances" (Ensemble de Servitude), "5" à "50 Fuite" (Ensemble du Calculateur), "15/20/25/30 Dommages Critiques" (Ensemble du Chanceux) — malgré le passage à `BonusPalier`/icônes du chantier précédent, ces lignes tombaient encore dans le fallback texte brut (`notes`).
- **Cause racine, 2 bugs distincts dans `parserBonusTexte` (`generate-ensembles.js`)** :
  1. Regex de découpe `/^([+-]?\d+.../` exigeait le signe collé au chiffre — cassait sur du texte source avec un espace après le signe (`"+ 5 Fuite"`, espacement incohérent dans `Liste-des-ensembles.md`). Corrigé : `/^([+-]?)\s*(\d+(?:[.,]\d+)?)\s*%?\s*(.*)$/`, signe et chiffres capturés séparément, espace toléré entre les deux.
  2. "% résistances distance/mêlée" n'avait **aucune branche dédiée** dans le parseur — tombait directement dans le fallback `notes` (texte brut), alors que le concept est volontairement indicatif (aucun impact calcul, cf. `sorts-degats-indirects.md`) mais n'avait pas à s'afficher différemment des autres lignes pour autant.
- **Fix** : nouvelle catégorie de delta **`indicatifs`** (`generate-ensembles.js`) — structurée comme `statsPlates`/`multiplicateurs` (donc affichable avec icône) mais sans aucune clé de stat associée, pour rester fonctionnellement inerte comme prévu. Nouvelle table `ICONES_INDICATIFS` (`client/src/constants/iconesEnsembles.js`, 🛡️ Rés. Distance/Rés. Mêlée) + nouvelle branche de rendu dans `BonusPalier.jsx` (après `multiplicateurs`, avant `notes`) — un seul composant partagé, donc `EnsembleDetail.jsx` et `EnsemblesPersonnage.jsx` en profitent sans changement supplémentaire.
- **Vérifié** : sweep complet des 29 ensembles régénérés (`node scripts/generate-ensembles.js`) confirmant zéro `notes` inattendue restante (seul "Aucun bonus" légitime survit dans `notes`), puis vérification live en navigateur des 4 cas signalés (clic sur chaque card `/ensembles`, lecture DOM des lignes `.bonus-palier__ligne`) : les 4 confirmés avec icône, sans "+", valeur correcte.

### ✅ Vignettes d'ensembles cassées après un redéploiement + stats manquantes sur les ensembles de cubes — corrigés
- **Vignettes disparues** : signalé juste après un déploiement du porteur de projet lui-même — les 29 images d'ensemble n'apparaissaient plus sur `/ensembles`. Cause : `generate-ensembles.js` reconstruit tout `ensembles.json` à partir de zéro à chaque exécution, mais le champ `imageComposite` n'était ajouté qu'après coup par un script séparé (`generate-ensembles-images.js`, lancé une seule fois lors d'un chantier antérieur) — jamais rejoué lors du dernier fix de texte (regex/`indicatifs`), donc le JSON régénéré puis commité/déployé ne contenait plus ce champ, alors que les 29 fichiers `.webp` étaient restés intacts sur le disque. **Fix définitif** (pas un rattrapage ponctuel) : `generate-ensembles.js` calcule désormais lui-même `imageComposite` à chaque exécution — chemin déterministe `/images/ensembles/<cle>.webp`, vérifié sur disque (`fs.existsSync`) — impossible que ce champ disparaisse à nouveau au prochain regen, quelle qu'en soit la raison.
- **Stats manquantes sur les 5 ensembles de cubes** : signalé juste après (capture "Ensemble de Cubes Air" ne montrant que Vitalité/Puissance au lieu des 3-5 stats attendues par palier). Cause distincte : les paliers `PANOPLIES` (`calcul.js`) mélangent des stats déjà connues de `ICONES_ENSEMBLES` (VITALITE, PUISSANCE) avec des stats jamais ajoutées à cette table (AGILITE/FORCE/INTELLIGENCE/CHANCE, DO_AIR/DO_TERRE/DO_FEU/DO_EAU, DOMMAGES) — absentes uniquement parce que les ensembles classiques/boss (seuls testés jusqu'ici) ne les utilisent pas. `BonusPalier.jsx` écartait silencieusement toute clé sans config (`if (!config) return null`), donc ces stats disparaissaient sans la moindre erreur ni log.
  - Les 9 clés manquantes ajoutées à `ICONES_ENSEMBLES` (`iconesEnsembles.js`), mêmes icônes/libellés que `StatsPersonnage.jsx` (FORCE/INTELLIGENCE/CHANCE/AGILITE) et `PanopliesPersonnage.jsx` (DOMMAGES/DO_*, déjà résolu là-bas pour ce même problème — aurait dû être copié en entier dès le départ).
  - **Fix systémique** en plus du rattrapage de données : les 3 boucles de `BonusPalier.jsx` (statsPlates/multiplicateurs/indicatifs) ont maintenant un repli générique (icône 🔹, libellé = clé brute) au lieu d'un `return null` silencieux — une stat future non répertoriée s'affichera moche mais visible, jamais invisible sans trace.
- Vérifié en direct sur `dedalofus.fr` après chaque déploiement (clic sur les cards, lecture DOM des lignes) : 29/29 vignettes chargent (200), Ensemble de Cubes Air/Feu affichent bien toutes leurs stats par palier.

### ✅ Détourage des 30 premières images de breuvages
- Le porteur de projet a fourni 30 captures d'écran des breuvages (`data/images/breuvages/`, non commitées — `.gitignore` racine exclut déjà tout `data/images/**/*.png`), chacune avec 2 problèmes à nettoyer avant de pouvoir les utiliser comme icônes du site (même exigence visuelle que les cubes/sorts : fond transparent, juste l'objet + ses étoiles de rang) : une **barre rouge d'UI** (barre de vie/ressource du jeu capturée par erreur, toujours collée en haut) et un **cadre/fond sombre** de tooltip autour de l'icône.
- **Étape 1 — retrait de la barre rouge** (`server/scripts/nettoyer-barre-breuvages.js`, Node/`sharp`, rejouable) : détection **géométrique** (bande pleine largeur collée à `y=0`) plutôt que par simple dominante de couleur — un rouge "pur" (vert et bleu faibles ET proches l'un de l'autre) distingue la barre d'un dégradé ambré/doré de bouteille qui a aussi du rouge dominant mais où le vert reste une fraction significative (sinon la détection mangeait le haut des fioles dorées, ex: `153103`/`152855`/`153413`, jusqu'à 40px de hauteur au lieu de ~15px). Zone de la barre repeinte avec la couleur de fond réelle échantillonnée juste en dessous. 2 images (`153517`/`153527`) étaient déjà recadrées sans barre par le porteur de projet — détection correctement négative, copiées telles quelles.
- **Étape 2 — détourage du fond/cadre en transparence** (`server/scripts/detourer-breuvages.py`, **Python**, premier script Python du projet — jusqu'ici 100% Node) : le flood-fill par couleur déjà utilisé pour breloques/sorts/ensembles (`generate-ensembles-images.js`) a été testé en premier par cohérence avec l'existant, mais **échoue nettement** sur ce style d'icône — dégradés/lueurs trop progressifs vers le fond (pas de rupture nette de couleur comme sur les breloques/sorts), la tolérance de couleur en chaîne finit par manger une bonne partie du breuvage lui-même (vérifié : le coeur rouge et la chope rouge se retrouvaient presque entièrement effacés, ne laissant que quelques reflets). Remplacé par **rembg** (modèle IA de segmentation `bria-rmbg`, exécuté **localement**, pas d'upload vers un service tiers) : `pip install rembg[cpu]`, modèle ~1 Go téléchargé une fois puis mis en cache — résultat net, breuvage + étoiles intacts sur les 30, y compris les cas rouges qui cassaient le flood-fill. Marges transparentes ensuite recadrées (`PIL.Image.getbbox`) dans le même script.
- **Étape 3 — reste de cadre collé à l'objet** (`server/scripts/nettoyer-restes-cadre-breuvages.js`, Node/`sharp`, rejouable/idempotent) : repéré après coup par le porteur de projet sur le coeur rouge (`152834`) — un bandeau noir opaque bien visible restait collé à l'image malgré l'étape 2, à l'endroit où le bouchon dépasse dans un coin du tooltip d'origine (frontière objet/fond ambiguë pour rembg à cet endroit précis). 2 pièges rencontrés avant d'arriver à un résultat propre sur les 30 : (1) une 1ère tentative de nettoyage par « composantes connexes + rejet si couleur moyenne quasi-noire » ne fonctionne pas ici car le reste de cadre est **fusionné en une seule composante** avec l'objet (pas de coupure nette) — la comparaison ne peut alors se faire que par flood-fill géométrique depuis les bords, comme aux étapes précédentes ; (2) le fond du jeu a 2 tons distincts (fond intérieur ~26/255, anneau du cadre vers les coins ~55/255, magnitude ~90) — un seul flood-fill à seuil unique laissait passer l'anneau plus clair ou mangeait l'objet selon le seuil choisi ; corrigé avec **2 seuils successifs (60 puis 100), chacun rejoué en boucle avec recadrage entre les passes** (un reste encore interne après une passe peut se retrouver exactement sur le nouveau bord une fois recadré, et devenir attrapable à la passe suivante — une seule passe ne suffit pas toujours à converger). Vérifié par relecture visuelle des 30 images une par une (pas seulement un comptage de pixels, qui donnait de faux positifs sur les teintes sombres légitimes de certains breuvages — flaque violette, ombre du coeur) : plus aucun reste de cadre nulle part.
- Résultat dans `data/images/breuvages-final/` (30 fichiers, non commités comme le reste de `data/images/`).
- ⚠️ **3 images ont un artefact indépendant de la barre rouge**, pas corrigé automatiquement (une fine ligne colorée traverse l'icône, ne touchant pas les bords — le flood-fill géométrique de l'étape 1 ne la voit pas, et rembg la garde car elle est "collée" au sujet détecté, pas au fond) : `152809` (ligne bleue), `152834` (ligne blanche), `152917` (ligne violette). Probablement un reste de sélection/annotation de l'outil utilisé par le porteur de projet pour découper les captures, pas un élément du jeu. À corriger à la main (retouche ponctuelle) si ces 3 images sont utilisées telles quelles.
- Les étoiles visibles sur les captures (1/2/3 selon le rang) ont ensuite confirmé l'hypothèse déjà notée d'un système de rang à étoiles pour ce type d'équipement — voir "Onglet Breuvages" ci-dessous pour la suite (table, API, page liste, branchement fiche perso).

### ✅ Onglet Breuvages — 4e type d'équipement entièrement branché (table, API, page liste, fiche perso, calcul)
- Une fois les 30 images détourées (chantier précédent), le porteur de projet a fourni directement en conversation les noms + stats des 10 familles de breuvage (3 rangs chacune, **Petit/Normal/Grand** — pas de 4e rang comme les autres types) : chaque breuvage ne boost qu'**une seule stat fixe**, aucune logique conditionnelle comme les breloques.

  | Famille | Stat (`cle_stat`) | Petit / Normal / Grand |
  |---|---|---|
  | Stimulant | INTELLIGENCE | 50 / 100 / 150 |
  | Fortifiant | FORCE | 50 / 100 / 150 |
  | de Réussite | CHANCE | 50 / 100 / 150 |
  | Assouplissant | AGILITE | 50 / 100 / 150 |
  | de Renfort de Santé | VITALITE | 100 / 250 / 400 |
  | Raide Boule | PUISSANCE | 40 / 80 / 120 |
  | Pétillant | DO_CRIT | 10 / 20 / 30 |
  | Percutant | DO_POU | 20 / 40 / 60 |
  | Vaporeux | FUITE | 40 / 60 / 80 |
  | Sirupeux | TACLE | 30 / 60 / 90 |

- **Les 10 clés de stat existaient déjà toutes** côté calcul (`INTELLIGENCE`/`FORCE`/`CHANCE`/`AGILITE`/`VITALITE`/`PUISSANCE` génériques, `DO_CRIT`/`DO_POU` déjà sommés génériquement, `FUITE`/`TACLE` déjà lus par les formules `FUITE_TOTALE`/`TACLE_TOTAL`) — **zéro nouvelle formule à écrire dans `calcul.js`**, juste un point de fusion supplémentaire.
- **Schéma** (`schema.sql`) : table `Breuvage` (id, nom, rang, cle_stat, valeur_stat, image_url — une seule stat par ligne, contrairement à `Cube`/`StatCube` qui ont une table séparée pour un nombre de stats variable, ici toujours 1) et `EquipementBreuvage` (3 emplacements, même schéma que `EquipementBreloque`/`EquipementSort` mais sans colonne `boost_valeur`, pas de bonus conditionnel ici). Migration `server/scripts/migrate-breuvages.js` (rejouable) : crée les 2 tables si absentes et rattrape les personnages déjà existants (19 en local) avec leurs 3 emplacements vides — les nouveaux personnages les reçoivent directement depuis `creerPersonnage`.
- **Import** `server/scripts/import-breuvages.js` : pas de CSV source (data communiquée directement en conversation), donc table `BREUVAGES` codée en dur dans le script plutôt qu'un fichier intermédiaire pour seulement 30 lignes. Convention de nommage des fichiers sources détectée automatiquement (`breuvage-<slug>.png` / `petit-breuvage-<slug>.png` / `grand-breuvage-<slug>.png`, 1 exception gérée en dur : `petit-preuvage-assouplissant.png`, coquille "preuvage" dans le nom fourni). Images converties en webp et copiées vers `client/public/images/breuvages/<id>.webp`, comme les autres types. **30/30 lignes insérées, 30/30 images rattachées** (aucun avertissement d'image manquante).
- **Backend** (`personnagesController.js`) : `construireFichePersonnage` requête les 3 `EquipementBreuvage`, agrège leurs bonus (`{ cle_stat: valeur_stat }`, sommés si jamais 2 breuvages boostaient la même stat) et les passe en **5ᵉ argument** à `calculerStatsPersonnage` (`bonusBreuvages`, même traitement additif que Parcho/breloques/ensembles, fusionné avant toute dérivation — donc `TACLE_TOTAL`/`FUITE_TOTALE`/`INITIATIVE_TOTALE`/`VITALITE_TOTALE` en profitent automatiquement sans code dédié). Nouvelles routes génériques `equiperBreuvage`/`equiperBreuvageAuto` (réutilisent directement les fonctions partagées `equiper`/`equiperAuto` déjà utilisées par sorts/breloques, `refTable: 'Breuvage'`) — la contrainte anti-doublon par `nom` existe techniquement mais ne se déclenche jamais en pratique (les 30 lignes ont chacune un nom distinct, ex: "Petit Breuvage Stimulant" ≠ "Breuvage Stimulant", donc rien n'empêche d'équiper 2 rangs du même breuvage à la fois — non demandé, pas ajouté). `desequiperTout` vide aussi les 3 emplacements breuvages désormais.
- **Frontend** : nouvelle page `/breuvages` (`BreuvageListPage.jsx`, consultable sans compte comme les 3 autres listes) + `BreuvageCard.jsx` (image + nom/rang + stat unique, ex: "+150 Intelligence") + `constants/rangsBreuvages.js` (`couleurRangBreuvage`, même convention que `couleurRangMaitrise` : Petit = bordure par défaut, Normal = argent, Grand = or). Lien "Breuvages (bientôt)" du header (`Header.jsx`) remplacé par un vrai lien. `PersonnageDetailPage.jsx`/`PartagePage.jsx` : les 3 cases `BREUVAGES_VIDES` statiques (jamais cliquables depuis leur création en Tâche 7) remplacées par le vrai mapping `personnage.breuvages`, exactement le même pattern que cubes/sorts/breloques (case vide → lien `/breuvages?perso=<id>`, case remplie → image + modale de détail + croix de déséquipement). `ModaleRemplacement.jsx` généralisé pour le 4ᵉ type (`bordureItem` bascule sur `couleurRangBreuvage` pour `type === 'breuvage'`).
- **Testé de bout en bout** : suite de requêtes API directes (personnage jetable créé/équipé/supprimé par script, rien laissé en base) confirmant équipement auto (1er emplacement libre), stats recalculées correctement (`TACLE_TOTAL`/`DO_CRIT` reflètent bien le bonus équipé, `VITALITE_TOTALE` = 1050 + 400 pour un Grand Breuvage de Renfort de Santé), erreur `409 COMPLET` au 4ᵉ breuvage (3 emplacements max bien respectés), page de partage publique cohérente, déséquipement fonctionnel. Vérifié aussi en navigateur (compte/personnage jetables, supprimés après coup) : filtre par rang sur `/breuvages`, clic sur emplacement vide → équipement → image affichée sur la fiche → clic → modale `BreuvageCard` → déséquipement → case revide, stats (`Intel.`/`Initiative`) mises à jour après rafraîchissement.
- **Étoile manquante récupérée avant l'import** : le rembg du chantier précédent avait fait disparaître l'unique étoile du "Petit Breuvage Sirupeux" (élément secondaire isolé, trop loin de l'objet principal pour être reconnu comme faisant partie du sujet par le modèle de segmentation) — repérée en comparant à l'image source brute (`data/images/breuvages/...153139...`), l'étoile a été redétourée isolément par flood-fill (fond simple, pas de dégradé à cet endroit) puis recomposée à la bonne position relative (calculée depuis les coordonnées de l'image source, pas devinée) sur l'image finale avant l'import.

### ✅ Retouches breuvages (taille + bordure) et embed Discord
- **Retours du porteur de projet** après un premier aperçu de l'onglet Breuvages : les images étaient trop grandes dans leurs cases (débordaient jusqu'aux bords), et la bordure colorée par rang (argent/or, calquée sur `couleurRangMaitrise`) ne devait pas exister — juste retirée, sans remplacement.
  - **Cause de la taille** : `EmplacementSlot.css` applique `transform: scale(1.35)` à toutes les images de case, pour compenser une marge noire intégrée assez large sur les images sorts/breloques existantes. Les images de breuvages, elles, sont détourées **au plus près du sujet** (pipeline de détourage + recadrage `getbbox()`, voir plus haut) — le même zoom les fait déborder. Nouvelle prop `imagePetite` sur `EmplacementSlot.jsx` (classe `emplacement-slot__image--petite`, `scale(0.85)` au lieu de `1.35`), branchée uniquement sur les breuvages (`PersonnageDetailPage.jsx`/`PartagePage.jsx`/`ModaleRemplacement.jsx`).
  - **Bordure retirée** : prop `sansBordure` (déjà utilisée par les cubes) branchée aussi sur les breuvages dans les 3 mêmes fichiers — `couleurRangBreuvage`/`COULEURS_RANG_BREUVAGE` devenus inutiles, supprimés de `constants/rangsBreuvages.js` (ne garde que `RANGS_BREUVAGES`, toujours utilisé par le filtre de `/breuvages`).
- **Embed Discord (`server/routes/ogImage.js`)** : vérifié à la demande du porteur de projet — **les breuvages n'apparaissaient pas du tout** sur l'aperçu (tableau `breuvages` codé en dur à `[null, null, null]`, jamais branché lors de la création initiale de ce générateur, avant même l'existence de la table `Breuvage`). Corrigé : requête `EquipementBreuvage`/`Breuvage` ajoutée (même pattern que cubes/sorts/breloques), bordure `'transparent'` comme les cubes (cohérent avec le retrait de bordure ci-dessus).
- **Breloques en une seule ligne** (demandé au passage, l'embed était ouvert) : `creerGrille` remplacé par un simple `flex` sans retour à la ligne pour les 7 breloques (au lieu du split `slice(0,4)`/`slice(4)` en 2 lignes 4+3), avec un `marginTop: GAP_VERTICAL_BRELOQUES` (6px, plus petit que le `gap` normal entre icônes) pour séparer légèrement cette ligne du reste — le `gap` uniforme du conteneur parent (qui espaçait aussi les autres lignes) a été retiré pour ne garder que cet espacement précis, propre à cette séparation.
- **Vérifié en conditions réelles** : personnage jetable (cube + sort + 3 breuvages + 7 breloques distinctes, supprimé après coup) → image générée directement via `curl /api/og-image/<lienPartage>` et relue — breuvages bien visibles sans bordure, 7 breloques sur une seule ligne avec un espacement net mais discret par rapport à la grille du dessus. ⚠️ Le cache mémoire de ce générateur (`pngCache`, TTL 5 min, clé = `lienPartage`) a fait resservir une version périmée lors d'un premier test après avoir équipé de nouvelles breloques — à garder en tête pour de futurs tests manuels de ce endpoint (redémarrer le serveur ou attendre le TTL, pas de invalidation à la demande).
- **Effet de bord repéré en testant, sans rapport avec cette session** : le package `dotenv` insère parfois une ligne de "tip" promotionnelle (texte + URL, contenu différent à chaque exécution) **avant** la sortie normale d'un script `node -e` lancé en ligne de commande — capturée par erreur dans une substitution de commande bash (`TOKEN=$(node -e ... )`), elle corrompt la valeur récupérée. Repéré ici via un `401`/`400` sur un script de test ponctuel (jamais committé) ; contournable en filtrant la dernière ligne de la sortie (`| tail -n 1`). Sans impact sur l'application elle-même (jamais affiché aux joueurs), mais à garder en tête pour de futurs scripts ponctuels similaires lancés en ligne de commande.

## Points encore en suspens

- **Responsive mobile-first sur les pages déjà construites** (Tâche 5 : accueil, Cubes/Breloques/Sorts, Connexion/Inscription) : aucune media query pour l'instant, pas testé sur petit écran. La convention mobile-first ne s'applique qu'au code écrit *à partir de* la Tâche 7 — une passe dédiée reste à faire sur l'existant.
- **Vulnérabilités npm (Dependabot)** : 9 signalées sur GitHub, dues au downgrade de Vite (8→5) et Vitest (4→2) pour compatibilité Node 18 **en local**. Dépendances de développement uniquement (jamais exposées en prod — le VPS tourne sur Node 22 et ne installe que les deps de prod côté serveur) — disparaîtront si le projet local passe un jour à Node 20+.
- **Redéploiement** : pas encore de procédure automatisée (CI/CD) — une mise à jour du code en prod nécessite pour l'instant une connexion SSH manuelle (`git pull`, rebuild client, `pm2 restart`). À formaliser si les mises à jour deviennent fréquentes.
- **`DO_CRIT` (bonus cube) et dommages poussée** : formules connues mais pas encore intégrées à `calculerDegats` (pas de modélisation du jet critique — se produit ou non — ni du nombre de cases de poussée pour l'instant) — pas urgent. La plage de dégâts critiques du **sort** lui-même (`degats_critique_min/max`) et le `%` critique total sont eux déjà calculés (Tâche 8, onglet Sorts).
- **Sourcing des images** des équipements : cubes (419/420, un seul manquant), breloques (166, dont 14 de rang `Unique` sans image — boss probablement pas encore illustrés) et sorts (493 lignes, dont 54 ajoutées via la comparaison touchmanager.fr) couverts pour ce qui existe déjà en base. **Breuvages** : 30 images sources détourées (voir "Détourage des 30 premières images de breuvages" ci-dessus), mais toujours **aucune donnée ni table** — 4e type d'équipement toujours pas alimenté côté base/API depuis sa création en Tâche 7, juste un premier lot d'images prêtes en attendant.
- **Décalage image_url / fichiers physiques sur les sorts** : 448 lignes `Sort` ont un `image_url` renseigné en base, mais seulement 244 fichiers existent réellement dans `client/public/images/sorts/` — repéré en vérifiant l'import des 54 sorts manquants (dont les 174 nouvelles lignes ont, elles, bien toutes leur fichier), donc un souci **antérieur**, pas encore diagnostiqué ni corrigé.
- **Doublon "Protection du Craqdoa"** (Ensemble du Phossile/Craqdoa) et annotation "Maître β uniquement" (3 sorts, Bousculeur/Calculateur) : à trancher par le porteur de projet (voir section "Page 'Ensembles'" ci-dessus).
- **Nouveaux sorts pas encore en base** : la comparaison avec touchmanager.fr (52 sorts + 2 ajoutés par le porteur de projet, voir "54 sorts manquants ajoutés" ci-dessus) a résolu la quasi-totalité du point signalé précédemment ici (~44 sorts repérés via des images sans correspondance à l'époque de "Premières images réelles") — pas de vérification exhaustive faite que 100% des cas sont couverts, mais le plus gros est fait. Le pendant côté breloques était déjà résolu (`DEDALE - BRELOQUES_NEW.csv`, voir "Onglet Boosts breloques").
